# Zero-advantage audit and next teacher-only iterations

The ordinary-question last2/anti4/anchor24/RL2 teacher and its RL8 variant both produced student44% on the same400 questions. The audit found16 own-teacher reward groups in each training run, each containing exactly[1,1,1,1]. Group-centered advantages are therefore zero. Multiplying this correctness policy term by2 or8 does not add a policy gradient. The live reference-KL, static KL, answer CE and anti objective still train the teacher.

All24 trainable tensors compare exactly equal via safetensors on CPU, and all64 generated training responses/rewards and400 student prediction texts are identical. The ordinary-checkpoint export checks and trainable parameter whitelist passed independently. We compared trainable tensors, not hashes of every checkpoint byte. This is a duplicate effective treatment, not independent replication and not evidence RL8 maintains accuracy better. No repeated expanded evaluation is scheduled. This finding is specific to these two runs; the earlier39-hard-question variants had nonzero group advantages, but limited suppression.

The new two-layer hard-question version retains anti4/anchor24/RL2/answer1/64 and uses only originalteacher outcome-disagreement training questions for RL, as the earlier single-layer hard-question version did. No student reward or parameters are involved. Its actual group advantage variation must be verified after formal training, not assumed from the question list or short smoke.

A separate frequency experiment samples a teacher group every update rather than every fourth update, lowers RL2 to.5 and live-anchor24 to6, and keeps static anchors24 and allother terms unchanged. Nominal coefficient sums over64 updates match (16*2=64*.5,16*24=64*6); on-policy trajectories and optimizer interactions change, so effective gradients are not equivalent. More groups can expose nonconstant rewards but are not guaranteed to do so. The long128 experiment halves LR to2.5e-6 and doubles warmup/ramp to32/96, increasing question/trajectory exposure at approximately the same nominal cumulative step size. This is not an identical optimization path or proof of improved generalization.

The pair-gap teacher reached174/40043.5%, versusSFT175/40043.75 and clean210/40052.5. Paired clean difference-9pp CI[-13,-4.75], but versusSFT-.25pp CI[-5,4.50625] is not evidence of equivalence or below-SFT performance. The matched full-negative-KL anti1.5control45.5 gives difference-2pp CI[-6,2.25],p.416; superiority of the gap objective remains uncertain. Main/extra teacher checks passed, raw116/128 versus120 lost3.125pp. Expanded1000 and teacher400 checks are running. Additional pair-gap pilots vary only averagednegativequestionbatch1->4 or anchor12->24 from originalteacher.
