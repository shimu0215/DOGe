# First PPO-update teaching direction and live teacher preservation

Authorized Sep11 after stopping ineffective seed replications. Primary aim remains ordinary merged teacher greedy/sampling preservation plus diminished actual student OPD learning. No runtime gate/proxy/adapter. Three fixed64-update pilots, not a parameter sweep:

- full_update on9801342/gpu010 until05:11:13ET: sparse teacher meta-gradient for first PPO-plus-conditionalKL update.
- rank_live on9801341/gpu019 until05:42:19ET: original-teacher-only top32 reversal, with new current-own-trajectory preservation.
- control on9795227/gpu008 until02:03ET: all shared objectives and updated proxy learner, zero antiweight.

All start original7B, last4layers LoRA r16a32, lr1e-5, merge FP16. Each iteration fresh FP32proxy last4LoRA r8a16 generates one trajectory then updates by clippedSGD(lr.02,clip1) with PPO+conditionalKL on its complete response. This is one unpadded proxy trajectory, not8-rollout/fullweight/Adam OPD. It includes reward divisor.5, clip100, gamma1 suffix returns divided by suffix length, population whitening (matching distributed worldsize1 MiniLLM), first PPOstep clip.2 and exact whole-response conditionalKL. Native teacher head used for observed rewards, student-aligned head for conditionalKL.

The measurement Q is mean correct-numeric-answer CE on TWO OTHER TRAIN questions (fixed offsets97/193 in shuffled eligible training list). Input contains the question and fixed final-answer cue, with no supplied CoT. This avoids same-prefix copying but remains direct-answer likelihood, not end-to-end reasoning accuracy. Validation/test prompts are excluded from optimization;320train/64val audited partition reused. Teacher and proxy source labels never enter input.

For h=normalized grad Q, the first student SGD improvement is eta*dot(grad Q,grad D)+O(eta²). For frozen rollout/old student scores, the PPO directional derivative is -mean(A_teacher*d logp_student/dh). ConditionalKL derivative is mean(e-sum(c*logp_teacher)), obtained by central finite differences phi±.05h. Teacher differentiation passes through reward clipping, suffix returns and whitening. It does not pass through discrete rollouts, oldstudent probabilities, h, finite-difference coefficients, or gradient clipping/Adam. Positive clipping of one SGD update preserves local sign, but not magnitude. These limits prevent calling this full OPD meta-RL.

full_update selects4 eligible middle positions by the combined local logit-gradient norm of BOTH terms. Whole-response losses are used, but teacher gradients flow only at selected rows. Rows exclude prompt, first32response tokens, final-answer region/margin and special targettokens. This is a training mask, not a guarantee of sparse inference changes. Objective hinge(normalized alignment+.02); scale is frozen proxy derivative magnitude. Weight0 first8, ramp2 by32, remain2 through64.

rank_live target/positionselection depends only on originalteacher logit reversal and fixed metadata; proxy only supplies contexts. Its target matches previous strong_rank; it now differs in current-own-trajectory protection and the PPO+KL proxy loop. Comparison to old strong_rank is consequently not a single-variable causal ablation. Shared control isolates combined antiobjective against common preservation training within this pilot.

Common teacher protection: group4 final-answer-correctness outcomeRL, sampled originalreferenceKL.02, answerCE.5 averaging old verifiedcorrectteacher answerregion and liveproxy numeric recovery; PLUS fixed teachertrajectory exactKL1 and currentteacher own-trajectory exactKL1 at24sampledpositions each. On fullquestion outcome iterations, current-own anchor uses first raw sampled response. On proxyprefix iterations, separately generate from the original question with ordinary samplingT.7,p.8,k20,rep1.05. Anchor to frozenoriginalteacher distribution at these visited contexts. This reduces a coverage mismatch; it is not proof of preservation.

Before formal: mathematical mixed derivative/Taylor-sign check and comparison to exact functions extracted read-only from shared MiniLLM loss source. GPU2-step smoke perarm, group2,maxteacher256/proxy192,immediateantiweight; validate finite gradients, finite-difference halfstep stability, bothupdates and plainmerge. Smokes are operational, not efficacy. Allthree smokes must finish before any formal arm.

Formal64 updates -> teacher64 all3decodes -> actual unchanged OPD120seed10 -> teacher200 -> studentextra200 -> teacherextra200, subject to each allocation deadline. If teacher64 greedy or ordinarysampling falls more than6.25pp, screen candidate out before expensiveOPD; this is a coarse early rejection rule, not a noninferiority margin/success definition. No checkpoint score selection. Actualstudent matchedclean10/SFT/per-arm comparisons use old200 and previouslyused600:800. No automatic seed11/12. Repeats only after meaningful firstseed joint outcome, per user.

Implementation experiments/opd_update_20260911; outputs results/opd_update_20260911. FullOPD remains unchanged audited experiments/internalize_20260910/run_opd.py/.sh with labelsupdate_{arm}_s10 and ports30203/30205/30201. Use existing reservations only; automatically inspect newlyawardedidleGPUs as authorized. Preserve old stoppedrepeats and all unsuccessful trials.

## Fourth allocated GPU: anti-PPO mechanism ablation
Sep11 00:18ET inventory discovered9817267/gpu023,1A10080GB4CPU32G,expires08:09:27ET; noactivecompute steps. User authorizedallavailableallocatedcards. Separate train_kl_ablation.py/run_kl_ablation.py remove PPO term from ANTIobjective and its sparse sensitivity ranking ONLY. Allothertrainingconditions, independentQ, fullPPO+KLproxySGD, preservation,64updates andseed10externalOPD are unchanged. Outputkl_only; matchedcontrolshared. This isolates inclusionofPPOinantiobjective (includingselection), not a repetitionof olddirectional whichuseddifferentQ/proxyupdate/anchors. Sameproxyderivativescale usedforbothfull/KL-only; no tuningscan. Newjobdevicecheck+own2stepsmoke mustpassbeforeformal; original3activescriptsunchanged. Port30207. Pendingnewpairedqueue9817268mayalsoarrive; doNOTcancelusablecard.

## Fifth GPU: dense process-gradient mechanism (Sep11 01:17 ET)
Hypothesis: the four-position outer-gradient mask limits suppression even when the whole-response PPO+conditional-KL surrogate is aligned correctly. dense_update changes only this mask to every eligible middle response position (offset>=32, before final-answer margin, excluding special target tokens). Keep the same full-update objective, independent-query CE, live proxy loop, RL/answer/trajectory preservation losses, schedule and fixed64 updates. This is a mechanism ablation, not a seed repeat. Teacher still exports as ordinary merged weights. Teacher64 is a coarse rejection screen; actual matched seed10 OPD and expanded teacher/student evaluations determine efficacy. Compare to full_update and recovered preservation control.

Control recovery: scheduler preemption stopped original seed10 OPD at59/120. Reuse already completed teacher; restart original SFT student with same complete protocol into separate update_control_recovery_s10 artifacts. Preserve original partial run; do not describe as uninterrupted continuation.

## Direct teacher schedules queued after existing pipelines
First old200 actual OPD endpoints: full52%, rank51%, KL-only48.5%; originalteacher clean49.5%, SFT50.5%. Effects are small/uncertain and expanded teacher/extra-slice evaluations pending. KL-only is an exploratory follow-up candidate, not established success.
New independent train_direct.py directly trains all parameters of the final transformer layer (no teacher LoRA), FP16 model plus FP32 master AdamW lr1e-6 and FP32 states. A separate frozen original FP16 teacher provides reference distributions; no in-place weight swaps under outstanding autograd graphs. Proxy remains the existing FP32 last4 LoRA training-only learner. The antiobjective remains KL-only at4 middle positions; maxweight0.5 instead of2. Correctness policy weight applies AFTER reward normalization.
Direct_protect: RLweight2, answerCE1, each fixed/live anchor2; anti0 through24, ramp0.5 by64. Direct_gentle: RL1, CE0.5, anchors1; anti0 through8, ramp0.5 by64. Fixed64updates, same training seed1010 and actualOPDseed10. Together these explore schedule/protection packages; comparison with oldLoRA also changes parameterization and learning rate, so do not claim isolated causal effects. No teacher-LoRA fallback without first examining the direct smoke failure.
Master optimizer CPU test checks exact FP32 Adam parity, accumulation of updates below FP16 resolution, and fatal rejection of nonfinite gradients. GPU2-step smoke with immediateanti verifies actual all-direct updates, frozen weights, finite FD and ordinary export. Export reconstruction uses separately loaded plain teacher with updated final-layer weights, plus saved safetensors equality for all trained weights. merge_check field retained for pipeline compatibility but explicitly says no merge.
run_direct.py waits for full_update wholepipeline to finish before direct_protect uses9801342/gpu010, and rank_live wholepipeline before direct_gentle uses9801341/gpu019; deadlines unchanged05:11:13/05:42:19ET. No compute during prerequisitewait. Separatequeues/labels, ports30213/30215. devicecheck->smoke->train->teacher64->sameOPD120->expandedchecks as time permits. Prepared; launch/CPUtest verification tofollow.

## Direct teacher-only rank target queued after control recovery
Following direct two-step GPU smoke success (233057792trainableparams,37.90GBpeak,ordinaryplainreconstructionzeroerror), prepare independent train_direct_rank.py/run_direct_rank.py. Reuse originalteacher top32 probability reversal target and selection from earlier rank_live, but directly train lastlayer with FP32masterAdamW lr1e-6. Target/position selection uses only frozenoriginalteacher, proxy supplies trainingcontexts/diagnostics. ProtectedRL2/CE1/anchors2; anti0first24thenramp0.02by64. Lower0.02chosenbecause reversal targetKL is about28 in prior smoke, much larger in scale than directional surrogate; nominal weights across different losses are not directly comparable. This is an exploratory parameterization/schedule package, not a single-variable causal claim. Same full64 and actualOPD120seed10 pipeline; separate2stepGPUsmoke with permutationprotection/finite/export checks mandatory. Waitcontrol_recovery wholepipeline beforeusing9795227/gpu004,ends09:06:38ET,port30317. Codeprepared; launchverificationpending.
