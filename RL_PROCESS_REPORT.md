# 首轮 outcome RL + 冻结 proxy 过程 KL：最终结果

2026-09-10 21:48 ET，全部计划评测和合并后信号审计完成。**这轮没有证明有效削弱 OPD，也没有证明 teacher 性能完全保持。** 后续三卡闭环实验是另一轮，记录在 LEARNING_LOOP_DESIGN.md / LEARNING_LOOP_STATUS.md。

两组从原始 Qwen2.5-7B 开始，最后四层 rank16 LoRA，128 次更新，最终合并成普通 FP16 权重。联合组为答案正确性 RL + 参考 KL + 0.25 KL(冻结 SFT proxy || teacher)；对照组只有相同的 RL 和参考 KL。这里削弱过程信号的目标是辅助 logit loss，不是完整学生训练收益作为 RL 奖励。推理不调用 proxy 或外挂组件。

## 真实 OPD120 后的学生

| teacher | GSM 旧200题 | GSM 600:800额外200题 |
|---|---:|---:|
| SFT student，未做OPD | 50.5% | 50.0% |
| 原 teacher，OPD seed10 | 49.5% | 51.5% |
| RL + 过程KL | 50.5% | 51.5% |
| 仅答案 RL | 51.0% | 53.0% |

联合组相对原 teacher 的配对差异：旧集 +1pp，95% bootstrap CI [-4,6]；额外集 0pp，CI [-5,5]。联合组相对仅答案 RL：旧集 -0.5pp，CI [-6,5]。均不支持已实现抑制。学生达到 SFT 水平不是充分证据，因为原 teacher 的 OPD 收益本身不稳定。

所有实际 OPD 使用原先审计后的120步、seed10流程；训练代码哈希验证通过。其他历史 clean seeds 有不同收益，不能挑选其中最好成绩作为这一轮的匹配对照。两个测试切片以前已经使用，不是全新未触碰测试集。

## Teacher 自身正确率

每个单元依次为 greedy / 普通 sampling / raw sampling。

| 模型 | 旧200题 | 额外200题 |
|---|---|---|
| 原 teacher | 90 / 90.5 / 88 | 91.5 / 91.5 / 91 |
| RL + 过程KL | 91.5 / 89.5 / 86.5 | 92 / 90 / 92 |
| 仅答案 RL | 91 / 90 / 89 | 91.5 / 92 / 90 |

联合组的普通 sampling 点估计分别下降1和1.5pp。区间包含零不等于已经证明非劣，因此不宣称满足 teacher 不掉点要求。

## 合并后的过程信号

同样的24个训练验证问题、同样的过程位置，直接加载合并后的普通 teacher：

| 量 | 原teacher | 联合组 | 仅答案RL |
|---|---:|---:|---:|
| KL(proxy || teacher) | 0.174671 | 0.158932 | 0.174968 |
| 已采样token评分差RMS | 0.652709 | 0.588736 | 0.653580 |
| 条件KL对proxy logits梯度的平均L2范数 | 0.084326 | 0.074716 | 0.084733 |

联合组 KL 减少约9.0%，logit梯度范数减少约11.4%；仍有相当信号。合并前后联合组 KL 只差0.0000244，说明未见信号削弱被合并过程抹掉。这里测的是条件 KL 对输出 logits 的梯度，不是学生参数梯度，也不是完整 PPO/OPD 更新。较小梯度并没有转化为实际学生收益下降。

原teacher输出头超出proxy头的平均概率质量约2e-8，这批位置上原生/对齐词表的已采样评分几乎相同。不能据此推广所有输入都没有差异。

## 运行记录

两组 LoRA 训练峰值约16.93GB，单张 A10080GB 足够。128个组中有奖励差异的组：联合15个，仅答案RL10个；很多组答案都正确，所以相对奖励的RL梯度稀疏。

结构化最终结果：results/rl_process_final.json。
远端所有原始评测、rollout、manifest及模型：/scratch/wzhao20/opd-gate-audit-run-20260909/results/rl_process_9795227。
实际学生OPD：results/internalize/rl9795227_{joint,outcome_only}_s10_*。
失败的前置检查保留；本轮正式训练/评测无失败或跳过阶段。
