# Relative-score positions and teacher correctness RL follow-up

The completed frequent-RL tail teacher gives175/40043.75%, equal to the SFT point estimate, versus clean210/40052.5%. Paired clean difference is-8.75pp (bootstrap95%[-13,-4.75], McNemarp=.00003885). Against the ordinary last2 tail176/40044%, the difference is only-.25pp (CI[-4.5,4],p1). Thus this is a promising combined treatment, not evidence of an independent RL advantage or equivalence to SFT. Its standard teacher checks and extra200 pass; raw128120/128 equals original120. Expanded student600 and teacher400 are now being evaluated.

Teacher own correctness RL every step yields9nonconstant groups of64; difficulty-selected last2 yields2of16 and128-step training2of32. Their main students are43.75/45.75/46.25 respectively. Unlike the earlier RL2/RL8 duplicate with all-zero advantages, these do have nonzero correctness advantages, but the results do not establish RL causality. No student parameter, logits, gradients or reward enters any teacher update.

The previous target-fit diagnostic suggests why a large relative-score loss reduction may not suffice. Top2-swapped targets select positions with very large original divergence (heldout negative mean17.59064), yet gap teacher nativeKL is only.003399 and argmax flips0. The head version nativeKL is approximately5.07e-7, with flips0. Strong batch4 fit perturbs originalteacher contexts substantially too. These are selected positions on fixed offline prefixes, not an OPD causal assay. The exposure sidecar must accompany train/heldout labels: failed batch4 saw none of the32reference-trained negative question IDs. Different target families select different positions.

For any two tokens a,b, log p(a|h)-log p(b|h)=z_a(h)-z_b(h). Fitting this gap removes the softmax normalization from that scalar objective, but does not guarantee a larger change in observed-token reward, correctness or student learning. A gap can shrink from20to12 while the preferred token remains extremely likely. Huber has a saturated derivative outside its quadratic region; clipping a faraway target can leave early gradients unchanged. The capped batch4 pilot has the same reported teacher scores as the uncapped failed pilot, including raw47/128; identical parameters have NOT been verified and should not be claimed.

Two new last-layer experiments therefore change what is fitted rather than only the strength:

1. Top2 gap loss on positions where the originalteacher argmax differs from the observed token in fixed offline text. Sample up to32 process positions aftertoken32; select up to8active positions by teacher-derived targetKL. No disagreements means zero negative loss, while positive and correctness terms still apply. Argmax disagreement is not a label of mathematical error.
2. The existing bounded observed-token pair target (cap4) fitted with Huber pair-gap rather than its prior full-vocabularyKL. The pair contains originalteacher preferred and observed offline token. The target preserves pair mass and protected mass, increases observed-token probability, and has nondecreasing entropy; it does not guarantee teacher quality or negative OPD gain. This is distinct from the already tested fullKL observed-pair/floor variants; it is not claimed to be a new transformation.

Both use fresh original7B, lastlayer,64updates, anti1.5/anchor24/answer1/ownRL2 every4, warm16/ramp48. Originalteacher alone generates scores; fixed offline text supplies token IDs. Plain checkpoint export is checked. Independent student OPD starts only after teacher weights are fixed and main quality passes.

Two other variants reuse the frequent-RL recipe and change only anti4to6 or correctnessRL.5to2 respectively. Staticanchor24/liveanchor6, every-step group4, last2/64 remain unchanged. A larger correctness weight can now affect nonzero groups, but benefit is still a hypothesis. This is not proxy optimization, a multi-seed confirmation, or demonstrated cross-student transfer.

CPU join of the diagnostic's decoded token strings finds248/248 heldout-negative Top2-selected positions match the observed offline token string; tail selection matches168/248. Token ID equality was not saved in the earlier diagnostic and is not inferred from decoded equality. This supports investigating disagreement positions, not a claim about mathematical correctness.

The first two new position trainers failed at formal update21 on an empty active set: zero loss was constructed under no_grad and then backpropagated. Their initial two-step smoke had not encountered this branch. V2 returnsNone and explicitly skips only the negative backward on empty selection; all other losses continue. Failures are preserved, fresh runs start from originalteacher, and successful later formal updates are needed to validate the fix. No formal v1 checkpoint is used.

The subsequent exact1000-question aggregate gives frequentRL47.3%, clean54.1%, SFT43.5%. Clean difference-6.8pp (CI[-9.6,-4.1]); residual3.8pp (CI[.8,6.8]). Difficulty-selected last2 is49.1%. The apparent400-question elimination therefore does not persist in this larger adaptive evaluation. Expanded teacher checks are still pending at this entry. The combination remains a partial-suppression candidate, not a solved defense or demonstrated independent RL contribution.

Expanded teacher checks now complete for frequentRL: on the added400, greedy91%=original91%; standard sampling89% versus89.5%, within the existing1pp per-slice tolerance. Its exact paired1000 teacher aggregate is915/1000greedy versus original906, and898/1000sampling versus original896. Greedy difference+.9pp CI[-.3,2.2]; sampling+.2pp CI[-1.3,1.7]. These descriptive checks support preservation on the measured settings, not a pre-registered statistical noninferiority claim. Student1000 remains47.3versus43.5SFT/54.1cleanOPD.

A random-position diagnostic on the fixed heldout negative prefixes (31questions,992positions) finds93positions where the originalteacher argmax differs from the offline token. The frequentRL checkpoint raises the observed-token logprobability by a conditional mean81.6696/93=.8782 at these positions; the anti6 teacher raises it85.7374/93=.9219. On originalteacher heldout sampled prefixes,1024positions include36disagreements and988agreeing positions. The agreeing-position logprobability sums are-29.9859 and-35.4817 respectively (divide by988). This suggests stronger anti6 adds a modest observed-token uplift at disagreement positions while also perturbing teacher contexts more. It does not establish causal student harm, mislabel legitimate alternate tokens as errors, or prove source classification/generalization. Positions within a question are not independent statistical samples. The raw counts and sums are stored in the frozen diagnostic worker.

Two new staged repairs keepanti6 and change either staticanchor24to36 (linked liveanchor6to9), or answerCE1to4. Both retain ownRL.5every1,64steps,last2, originalteacher and offline-text-only negative inputs. Train+allteacher quality checks run first within the shorter allocation. Actual independent studentOPD still requires the entire staged teacher worker complete and standard per-slice quality pass; it is not yet performed for these repairs.

The further raw400 paired evaluation has now completed: originalteacher361/40090.25%, frequentRL343/40085.75%, difference-4.5pp (CI[-7.75,-1.25],McNemarp=.00956). This invalidates a broad claim of high-temperature preservation based on the earlier120/128tie. The ordinary greedy/standard-sampling1000 results remain unchanged; raw differs in temperature, top-p/top-k and repetition penalty. It is a supplementary robustness failure, not a retrospective change to the main admission screen.

A new frequent-hardRL pilot changes only the RL prompt distribution relative to frequentRL: use the39teacher-own outcome-disagreement training questions with valid correct anchors. It keeps anti4/staticanchor24/liveanchor6/answer1/ownRL.5every1/group4/64steps/last2. No student model or outcome is used. This combines previously separate frequency and own-difficulty hypotheses; efficacy and actual nonzero reward coverage must be measured. It optimizes correctness on the reweighted training distribution, not an unbiased full-dataset correctness objective.
