# Teacher 后训练固化实验报告

最终更新：2026-09-10 06:33 ET。四个现有 allocation 均已 TIMEOUT；没有新增或延长预约。

**本轮已实现不添加推理组件的普通 teacher 权重，但尚未验证同时满足“teacher 不掉点、稳定削弱 OPD 学生”的方案。** 最值得保留的是只改最后一层已有一个神经元的方案：teacher 表现基本保持，学生在两批题上的点估计均不高于 SFT；但相对同种子普通 OPD 的差异很小、置信区间跨零，不能把它宣布为有效防护。

## 1. 最接近目标的结果

均为 GSM8K 正确率（%）；学生是 14B CoT SFT 后的 Qwen2.5-0.5B-Instruct，teacher 为 Qwen2.5-7B-Instruct。下表学生 OPD 均完成 120 步；同种子对照 seed10。

| 模型 / 流程 | 已探索的 test 0:200 | 预留的 test 600:800 |
|---|---:|---:|
| SFT 学生 | 50.5 | 50.0 |
| 原 teacher → OPD 学生 | 49.5 | 51.5 |
| 内置神经元 teacher → OPD 学生 | **48.5** | **50.0** |
| 原 teacher：greedy | 90.0 | 91.5 |
| 内置神经元 teacher：greedy | 90.0 | 91.5 |
| 原 teacher：常规 sampling | 90.5 | 91.5 |
| 内置神经元 teacher：常规 sampling | 90.0 | 91.5 |
| 原 teacher：完整 sampling | 88.0 | 91.0 |
| 内置神经元 teacher：完整 sampling | 87.5 | 91.0 |

预留题集三种 teacher 解码的**逐题正确与错误向量均相同**，不是仅总分相同。旧题集常规和完整采样各少答对一道题，不能声称严格零下降或普适不下降。

学生相对普通 OPD：

- 旧 200 题：−1 pp，配对 95% bootstrap 区间 [−6, 4]。
- 预留 200 题：−1.5 pp，区间 [−7, 4]。
- 两批合并 400 题的事后描述性统计：−1.25 pp，区间 [−4.75, 2.25]；相对 SFT 为 −1 pp，区间 [−5.75, 3.5]。

合并题目不增加独立训练种子数。该候选只有一个完整 OPD 种子，不能证明效果稳定。预留集在权重固定后用于确认，没有据此重新调参。

## 2. 内置神经元方案如何工作

Qwen 最后一层原有 SwiGLU MLP 的第 j 个神经元，对残差的贡献是：

`d_j · SiLU(g_jᵀx) · (u_jᵀx)`。

训练阶段冻结原 teacher，从成对的 teacher / proxy student 上下文收集该层输入 x。拟合来源方向 w，以及近似常数方向 c，使 `cᵀx ≈ 1`。然后直接替换**已有**神经元的三个向量：

`g_j = β(w − τc), u_j = c/β, d_j = d`。

其激活近似一个单边平滑函数：

`SiLU(β(wᵀx − τcᵀx)) · (cᵀx)/β`。

在 teacher 校准正例上，括号内明显为负，激活很小；学生风格足够强时才推高固定的数字干扰方向。d 来自原 teacher 数字“9”相对其他数字的输出方向，并按末层归一化尺度转换为残差方向。**修改目标不读取推理时的学生分布、模型或梯度。**

对 t≤−m<0，`|SiLU(t)|≤|t|exp(t)`；因此在给定间隔的缓存输入上，可以控制新增激活。这是局部激活界，**不保证**未见上下文上的 teacher 解题质量，也不保证 OPD 学生退步。替换已有神经元还会移除它原来的贡献；我们选择原贡献较小的单元，仍须实际自由生成验证。

本次选择单元 18356，β=80，τ≈10.482。只涉及原有 gate/up 的各一行和 down 的一列，共 10,752 个现有权重条目。生成时正常加载 Qwen 模型；训练采集 hook 已移除，不加载分类器、proxy、adapter 或自定义架构。

来源方向拟合使用 320 道训练题。阈值使用全部 384 道缓存题的正例校准，**包含来源分类验证题的正例**，因此校准正例几乎零激活不能解释成未见正例假阳性率为零。分类验证 AUROC 约 0.920，且正例包含查询和早期 token，不是纯 continuation 来源分类准确率。保留的学生负例中仅约 **1.43%** 位置明显激活；过于保守的覆盖是弱效果的一个可能解释，尚未做因果消融确认。

这是一种“训练后把拟合方向写回原有权重”的受约束后训练方法，并非普通端到端 SFT。符合无新增组件的结构要求，但不能因此视为防护目标已完成。

## 3. 其他方案及主要发现

| 方案 | 学生完整 OPD 后正确率 | teacher 自身情况 / 判断 |
|---|---:|---|
| 最后一层，top32 排序反转 | 45.0 | greedy 与 sampling 均退步 |
| 输出头＋最后两层，top32 反转 | 43.5 | 自身退步 |
| 子空间输出头 prefix-reset | 48.0 | 差异小；未达联合要求 |
| 最后两层，普通 token 拉平 | 27.0 | 采样退步 |
| 上项输出头乘 2 | 47.5 | 完整采样 75%，原版 88% |
| 上项输出头乘 4 | 51.0 | teacher 基本恢复，学生削弱也消失 |
| 仅原版输出头乘 4 对照 | 52.0 | 没有保护作用 |
| top32 拉平，输出头＋最后两层 | 33.0 | 自身采样退步 |
| 负例熵最大化 | 33.5 | 自身采样退步 |
| top32 拉平，完整合并 LoRA | 25.5 | 明显削弱学生，但 teacher 退步 |
| 上项半幅改变量 | 50.5 | 自身接近保持，未确认削弱 |
| 上项四分之一幅 | 49.0 | teacher 三模式 90.5 / 92 / 91；学生差异不显著 |
| 上项只保留末四层 | 52.5 | 完整采样 81.5%，未保住自身 |
| 双向 KL：prefix-reset | 43.5 | 自身退步 |
| 双向 KL：均匀目标 | 27.5 | 自身采样退步 |
| 闭式输出头修复 | 26.5 | 完整采样筛选仅 10.94% |
| 直接低秩 KL 输出头修复 | 27.5 | 完整采样筛选 25% |
| 扩充训练题＋尾部 KL 修复 | 25.5 | 完整采样筛选 23.44% |
| 末四层重新训练排序反转 | 21.0 | teacher 三模式约 57.81 / 59.38 / 53.13 |

表内 teacher “筛选”是 64 题，不能与 200 题分数当作同一端点；各模型完整记录见结果 JSON。所有表内学生都是相同旧 200 题、120 步、seed10。没有通过筛选的路线仍保留为失败实验，未用其学生下降单独宣称成功。

仅改数字输出头 10 行（35,840 个拟合系数）的另一条路线，完整幅度造成 teacher 数字循环，三种生成筛选均为 0%。缩至 25% 后，teacher 的完整 200 题为 **89 / 89 / 86.5**，原版 **90 / 90.5 / 88**，仍有 1–1.5 pp 的点估计下降。学生第 40 步为 51%，第 80 步为 50.5%；对应普通 OPD 均为 51%。第 120 步学生补评已完成，结果为 **53%**，高于 SFT 的 50.5% 和同种子普通 OPD 的 49.5%，因此没有达到削弱目标。相对普通 OPD +3.5 pp，95% 区间约 [−0.51, 8]。原 GPU024 到期打断驱动，已在 GPU025 从保存的 120 步权重恢复完整 200 题评测，并验证原训练代码哈希。原中断记录保留，没有伪造驱动完成状态。

数字头训练把全词表 KL 精确化简为数字项与非数字的归一化项，值和梯度恒等式检查通过。未修改非数字 logits，并不意味着非数字概率不变。该方案的低平均正例 KL 掩盖了关键数字决策位置的损伤；不能只用平均 KL 评价“保真”。

完整 LoRA 版本在 1,024-token 预算复测后，学生仍为 26%，匹配 SFT / 普通 OPD 为 51% / 50%。下降并非仅由 512-token 截断造成，但重复与长度变化仍存在，不能全部等同于知识丢失。

## 4. 基线、评测与可信度限制

普通 OPD 三个种子的正确率分别为：

| 种子 | 旧 200 题 | 预留 200 题 |
|---|---:|---:|
| 10 | 49.5 | 51.5 |
| 11 | 53.0 | 45.0 |
| 12 | 53.5 | 51.5 |

基线增益尚不稳定，故“学生不超过 SFT”不是充分的保护证据。候选只能与相同训练种子的普通 OPD 做主要成对比较，不能挑另一个种子最好的分数制造差异。

OPD：120 步，学习率 5e−7，batch 2，梯度累积 2，rollouts 8，max length 640，prompt 256，PPO 1，reward scale 0.5；teacher FP16，student BF16。三种 teacher 解码均固定 512 新 token：greedy，常规 T=0.7/top-p=0.8/top-k=20，以及完整 T=1/top-p=1/top-k=0。前两种 repetition penalty 1.05，完整采样为 1。保留解码设置、样本顺序、batch 随机种子与匹配对照。

统一以最后完整 boxed 数值、否则最后数值评分；处理合法 LaTeX 薄空格命令，所有候选与对照一致重算。旧版评分器分数与修正记录均保留。不会把 64 题、80 步、未完成评测或不同精度的补充实验冒充主要 200 题端点。

全部 1,000 条 OPD 训练提示和答案已与 GSM8K train 逐条核对，与 test 题目重合为零。早期 0:200 已反复用于探索，不能当作独立确认集。有限样本上“不显著下降”不等于等价或非劣性证明。

## 5. 推荐保留的研究方向

**保留内置神经元方案作为下一轮起点，不把它作为已验证的最终防护模型。** 它结构简洁、直接写入末层现有权重，且在预留 teacher 题集上保持三种解码表现。下一轮应先稳定普通 OPD 的增益，再固定候选规则、做多种子匹配实验。需要检验增加有效负例覆盖能否保留 teacher 自身表现，而不是继续凭小样本分数调阈值。

另一条尚未实施的方向是让 teacher 在正例上保持“任务正确”，而非严格复制所有原 token 分布：用正确 CoT 的监督或任务奖励约束，结合训练阶段 proxy 的实际学习效果优化负例目标。这可能提供更大的可行空间，但本轮没有实验证据，不能称为已有解决方案。

## 6. 产物

- 代码分支：`codex/spinda-gate-audit-20260909`；汇总代码提交 `f8ec993`。
- Hopper 实验根目录：`/scratch/wzhao20/opd-gate-audit-run-20260909`。
- 内置神经元 teacher：`results/internalize/existing_neuron/model`。
- 训练系数与选择冻结记录：`results/internalize/existing_neuron/training_coefficients.pt`、`existing_neuron_selection.json`。
- 数字头 25% teacher：`results/internalize/digit_head_a25_model`。
- 完整日志、原始生成、checkpoint、匹配比较与失败记录保留在远端 `results/internalize`。
- 本地汇总：[结果 JSON](/Users/shimu/Downloads/opd-gate-audit-20260909/results/internalize_final_summary.json)；[研究过程记录](/Users/shimu/Downloads/opd-gate-audit-20260909/INTERNALIZE_RESEARCH_LOG.md)。

## 7. 最终审计、迁移诊断与资源收尾

全量权重审计通过。与实际评测使用的 FP16 原 teacher 逐张量比较，339 个张量的名称、形状及架构配置一致，无 adapter 配置或自定义 auto_map。内置神经元版本只改变第 27 层 MLP gate/up 各一行和 down 一列，恰好 **10,752 个权重条目**。数字头 25% 版本只改变输出头数字行，实际变化 **35,823 个条目**，少于拟合系数数量是 FP16 舍入所致。审计记录包含实际变化位置，见结果 JSON 的 plain_weight_audit。

SVAMP 补充诊断使用官方数据导出的固定提示，所有比较均为 FP16、相同解码设置；学生权重来自 GSM8K 上的 SFT / OPD，**不是在 SVAMP 上重新训练 OPD**。

| 已完成的 teacher 端点 | 原版 | 内置神经元 |
|---|---:|---:|
| SVAMP 前 32 题 greedy | 93.75% | 93.75% |
| SVAMP 前 32 题常规 sampling | 93.75% | 93.75% |

两项的逐题正确性均相同。样本很小，只是探索性自身表现诊断。完整采样原版完成 16/32、候选完成 24/32；学生 SFT 完成 120/200、普通 OPD 与候选 OPD 各完成 128/200，均在 allocation 到期时中断。原始逐题输出已保留，**没有把部分评测记成完整 200 题结果**，也没有宣称跨数据集防护成立。尚未完成不同学生的匹配 OPD 验证；已准备的 1.5B 基线不能替代该验证。

调度器最终记录四个 allocation 均 TIMEOUT：gpu019 06:05:25、gpu029 06:17:27、gpu024 06:18:57、gpu025 06:32:59 ET；这些是包含退出清理的实际结束时间。任务持续使用现有配额至到期，未新增预约。主要 GSM8K 评测及数字头 120 步补评已完成，剩余 SVAMP 输出按部分结果归档。

**最终不推荐将任何一个模型宣布为成功的 OPD 防护方案。** 建议保留内置神经元 checkpoint 和数据作为下一轮最简起点；先解决普通 OPD 增益不稳定与有效负例覆盖不足，再做独立种子和新学生确认。
