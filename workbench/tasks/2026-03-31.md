# 2026-03-31

## codex-木守宫

- Checked `gpu022` and `gpu024` per user request with the rule that abnormal stops must be fixed, not only reported.
- Confirmed `gpu024` had actually completed all requested plain evaluations for the three 32B-derived students; earlier checks were misled by the wrong output filename pattern.
- Verified completed student eval summaries on Hopper:
  - `math32b_base_singletraj_basicdistill`: `MATH 347/500 = 69.4%`, `GSM-hard 229/500 = 45.8%`
  - `math32b_lambda0p2_singletraj_basicdistill`: `MATH 316/500 = 63.2%`, `GSM-hard 240/500 = 48.0%`
  - `math32b_lambda0p5_singletraj_basicdistill`: `MATH 330/499 = 66.13%`, `GSM-hard 243/500 = 48.6%`
- Confirmed `gpu022` old-version `14B lambda=0.2 seed42` recollection had stopped after `raw + score` but before `filter`.
- Repaired that abnormal stop by manually running `filter_agent_training_data` on the scored file:
  - `filtered=342`
  - `empty logs=0`
- Cleared `gpu022` resources after the repaired `lambda0.2` task and launched the next one-task old-version recollection:
  - `14B lambda=0.5 seed42`
  - launcher log: `/scratch/wzhao20/AgentDistill/logs/oldcollect_qwen3_14b_lambda0p5_seed42_launcher.log`
- User requested a real single-repo local project structure so future local/Hopper sync can happen through `AKDA2` only.
- Removed nested git metadata from:
  - `/Users/shimu/Downloads/DOGe-main/AgentDistill/.git`
  - `/Users/shimu/Downloads/DOGe-main/LlamaFactory-main/.git`
- Verified that local `AgentDistill` now resolves to the outer git root `/Users/shimu/Downloads/DOGe-main`; local structure is now one outer repo plus ordinary subdirectories.
- Verified Hopper-side source parity before merge work:
  - `AgentDistill` modified source files and the key modified `LlamaFactory-main` files matched local by SHA256.
  - `workbench` was not fully synced at first, so I resynced the whole local `workbench` into `/scratch/wzhao20/AKDA2`.
- Unified Hopper repo structure to match local:
  - removed `/scratch/wzhao20/AKDA2/AgentDistill/.git`
  - removed `/scratch/wzhao20/AKDA2/LlamaFactory-main/.git`
  - cleaned remote AppleDouble `._*` files under `/scratch/wzhao20/AKDA2`
  - verified Hopper now has only one git root: `/scratch/wzhao20/AKDA2/.git`
- Rechecked SHA256 after sync and confirmed Hopper now matches local for:
  - `workbench/AI_REGISTRY.md`
  - `workbench/DATA_REGISTRY.md`
  - `workbench/HUB.md`
  - `workbench/current/active_tasks.md`
  - `workbench/current/latest_handoff.md`
  - `workbench/tasks/2026-03-30.md`
  - `workbench/tasks/2026-03-31.md`
- Added `AgentDistill`, `LlamaFactory-main`, and `workbench` into the outer local repo and created commit:
  - `788a688` `unify AgentDistill, LlamaFactory-main, and workbench under AKDA2`
- Pushed `main` successfully.
- GitHub push reported that the repository has moved and the preferred new URL is:
  - `https://github.com/shimu0215/DOGe.git`
