# Latest Handoff

- Time: 2026-03-29 19:45 EDT
- Updated by: codex-木守宫
- Context: Hopper-side `Qwen3-32B` recollection continues on `gpu011`, while a new `Qwen3-14B` own-data entropy self-finetune has been launched on `6632119@gpu005`
- Decision:
  - Keep the `32B` recollection work on `gpu011`.
  - Use the newly active `6632119@gpu005` 4xA100 reservation for a separate `14B` entropy self-finetune that uses `14B`'s own trajectories, not the historical `14B model + 32B data` route.
- Why:
  - The original issue for seeds `45..56` was upstream collection failure, not filtering: raw/scored files existed but contained `Connection error` placeholders and produced `filtered=0`.
  - The rerun now uses the original AgentDistill native path with seed-selective rerun support plus near-complete recovery logic.
  - The previous recollection run had already advanced to:
    - `seed45=500`
    - `seed46=500`
    - `seed47=501`
    - `seed48=501`
    - `seed49=501`
    - `seed50=501`
    - `seed51=500`
    - `seed52=500`
    - `seed53=376`
    - `seed54..56` missing
  - When `6632118` became active, no recollection Python processes were still alive, so collection had to be resumed rather than merely monitored.
  - The continuation run was launched directly on `gpu011` with `SEED_LIST=53,54,55,56` using the same native AgentDistill rerun script.
  - New continuation launcher log: `/scratch/wzhao20/AgentDistill/logs/rerun_qwen32b_math_seed53_56_launcher.log`
  - Separately, the user requested a `14B` entropy run that uses `14B`'s own MATH trajectories and does not restrict each epoch to one trajectory per question.
  - A new script was added for that path: `/Users/shimu/Downloads/DOGe-main/AgentDistill/scripts/training/train_teacher_entropy_math_14b_owntraj_deepspeed.sh`
  - Its key behavior is:
    - `Qwen3-14B` raw logs as input
    - pre-train `raw/scored/filtered` summary printing
    - all filtered trajectories used as independent SFT samples
    - no `--random_trajectory_per_question`
    - lambdas `0.2 / 0.5 / 0.8`
  - That new job has been launched on `6632119@gpu005`, and the first active step is `score_answers` on the `Qwen3-14B` `seed42` raw log.
  - Important caveat: retries can create duplicate rows; seeds `47..50` currently have `501` lines and will likely need deduplication before downstream use.
- Next step:
  - Continue monitoring the active recollection on `gpu011` through seeds `53..56`.
  - Monitor the new `14B` own-data launcher on `gpu005` through its `score/filter` summary and then the three lambda training runs.
  - Once recollection is complete, inspect and deduplicate any overfull seed files before scoring/filtering/training.
  - If the current holds near expiry before work finishes, keep using whichever of `6632121` or `6632122` becomes active first as backup capacity.
- References: `/Users/shimu/Downloads/DOGe-main/workbench/tasks/2026-03-29.md`, `/Users/shimu/Downloads/DOGe-main/workbench/notes/hopper_agentdistill_experiment_record.md`, `/Users/shimu/Downloads/DOGe-main/workbench/dashboard/hopper_hold_node_direct_run.md`

---

- Time: 2026-03-30 00:05 EDT
- Updated by: codex-木守宫
- Context: `6632119@gpu005` is now being reused for two follow-up experiment lines: a smoke-test of the original student-distillation flow, and a queued `32B` own-data entropy FT path
- Decision:
  - Before starting new `32B` FT runs, first validate that the original student-distillation pipeline can run end-to-end on `MATH + python_only` using a fine-tuned teacher.
  - Keep the student path as plain SFT from teacher trajectories: no voting, no grouped multi-trajectory sampling, one teacher trajectory per question.
- Why:
  - The user wants downstream student experiments to use the most original/clean distillation path rather than AgentDistill-specific voting or grouped-sampling variants.
  - A smoke test with `Qwen3-14B lambda=0.2 -> Qwen3-0.6B` is the fastest way to validate that raw teacher recollection, `score/filter`, and student SFT all still connect cleanly.
  - `32B` own-data FT should only proceed after that smoke path is shown to run.
- Changes prepared locally and synced to Hopper:
  - New teacher recollection script for one-trajectory-per-question FT teachers:
    - `/Users/shimu/Downloads/DOGe-main/AgentDistill/scripts/inference/collect_teacher_math_python_singletraj.sh`
  - New original-style student distillation script:
    - `/Users/shimu/Downloads/DOGe-main/AgentDistill/scripts/training/train_student_from_single_teacher_math_python.sh`
  - New `32B` own-data entropy FT script:
    - `/Users/shimu/Downloads/DOGe-main/AgentDistill/scripts/training/train_teacher_entropy_math_32b_owntraj_deepspeed.sh`
  - Remote copies were written under `/scratch/wzhao20/AgentDistill/scripts/...`
- Active smoke run on `gpu005`:
  - launcher log:
    - `/scratch/wzhao20/AgentDistill/logs/student_smoke_math14b_lambda0p2_to_qwen3_0p6b_launcher.log`
  - teacher:
    - base model `Qwen/Qwen3-14B`
    - LoRA teacher `/scratch/wzhao20/AgentDistill/training_outputs/qwen3-14B/agent_baseline_2epochs_math14b_entropy_owntraj_ds_lambda0p2`
  - student:
    - `Qwen/Qwen3-0.6B`
    - plain SFT from filtered teacher trajectories
    - `EPOCHS=1`, `DATASET_SIZE=128` for smoke validation
  - latest verified state:
    - `vllm` teacher serve completed startup
    - `run_experiment` for FT `14B` teacher on `math_500_20250414.json`, `seed=42`, `python_only` is active on `gpu005`
    - raw output file has not completed yet, so student training has not started yet
- Next step:
  - Wait for the smoke run to finish teacher recollection and begin the student SFT stage.
  - If the student smoke run completes successfully, immediately start `train_teacher_entropy_math_32b_owntraj_deepspeed.sh` for `lambda=0.2/0.5/0.8`.
  - After `32B` FT completes, recollect single-trajectory `seed42` teacher data for the requested teacher set and run `Qwen3-0.6B` student distillation/evaluation on `MATH`.

---

- Time: 2026-03-30 (morning) EDT
- Updated by: claude-芽儿豆
- Context: Directory listing of `/scratch/wzhao20/AgentDistill/` on Hopper retrieved on demand.
- Result: `ls -la /scratch/wzhao20/AgentDistill/`

```
total 34
drwxr-sr-x  wzhao20  .
drwx--S---  wzhao20  ..
drwxr-sr-x  wzhao20  .git                (updated Mar 25 23:18)
-rw-r--r--  wzhao20  LICENSE             (Mar  7 22:02)
-rw-r--r--  wzhao20  Makefile            (Mar  7 22:02)
-rw-r--r--  wzhao20  README.md           (Mar  7 22:02)
-rw-r--r--  wzhao20  constraints.txt     (Mar  7 22:41)
drwxr-sr-x  wzhao20  data_processor/     (Mar  7 22:02)
-rw-r--r--  wzhao20  e2b.toml            (Mar  7 22:02)
drwxr-sr-x  wzhao20  examples/           (Mar  7 22:02)
drwxr-sr-x  wzhao20  exps_research/      (updated Mar 25 22:37)
drwxr-sr-x  wzhao20  images/             (Mar  7 22:02)
drwxr-sr-x  wzhao20  logs/               (updated Mar 30 00:00)  ← most recently touched
drwxr-sr-x  wzhao20  logs_slurm/         (Mar 25 12:01)
-rw-r--r--  wzhao20  pyproject.toml      (Mar  7 22:30)
drwxr-sr-x  wzhao20  scripts/            (Mar  7 22:02)
drwxr-sr-x  wzhao20  search/             (Mar  7 22:02)
-rw-r--r--  wzhao20  serve_vllm.py       (Mar  7 22:02)
-rw-r--r--  wzhao20  slurm_eval_teacher_agent_aime_olymath_qwen3_32b_14b_4gpu.slurm  (Mar 22 23:52)
-rwxr-xr-x  wzhao20  slurm_eval_teacher_agent_gsmhard_qwen2p5_1p5b_instruct_2gpu.slurm
-rwxr-xr-x  wzhao20  slurm_eval_teacher_agent_gsmhard_qwen3_1p7b_2gpu.slurm
drwxr-sr-x  wzhao20  src/                (Mar  7 22:10)
drwxr-sr-x  wzhao20  training_outputs/   (updated Mar 25 07:40)
```

- Key observations:
  - `logs/` was updated at `Mar 30 00:00` — consistent with overnight smoke-run activity.
  - `training_outputs/` last touched `Mar 25 07:40` — no new training output since then; smoke run student SFT not yet complete.
  - `exps_research/` last touched `Mar 25 22:37` — no new research experiment files since codex-木守宫's last sync.
  - Top-level `scripts/` has not been updated since repo clone (`Mar 7`) — remote script copies go under subdirectory, consistent with previous handoff notes.
- No action needed from this listing; recording for context continuity.

---

- Time: 2026-03-30 01:18 EDT
- Updated by: codex-木守宫
- Context: Repaired unattended Hopper execution after user requested no further permission prompts and confirmed `gpu005` should keep running 14B-teacher distill while `6632122@gpu009` should take over 32B FT then 32B-teacher distill.
- gpu005 (`6632119`, `gpu005`) status:
  - Shared launcher log `/scratch/wzhao20/AgentDistill/logs/run_qwen3_0p6b_from_14b_teachers_math_python_launcher.log` is active.
  - Current stage is still FT-14B(`lambda=0.2`) teacher recollection for `MATH/python_only/seed42`.
  - Progress observed at `177/500`; GPUs on `gpu005` were at `95-96%` util with ~`72.3GB` memory each.
  - Active remote processes include `run_qwen3_0p6b_from_14b_teachers_math_python.sh`, `serve_vllm.py`, `vllm serve Qwen/Qwen3-14B`, and `run_experiment ... --fine_tuned --lora_folder ...lambda0p2`.
- gpu009 (`6632122`, `gpu009`) repair:
  - Root issue was not launcher syntax; unattended chain had not stayed alive. Also, naive `pkill -f <script>` cleanup can kill the remote shell because the script name appears in the shell command line.
  - Confirmed `gpu009` basic SSH works (`hostname` returned `gpu009.orc.gmu.edu`).
  - Performed safe cleanup, then verified all four GPUs were idle (`1 MiB`, `0%`) and no relevant processes remained.
  - Relaunched unattended chain with:
    - `nohup bash scripts/training/launch_32b_ft_then_distill_batch.sh > /scratch/wzhao20/AgentDistill/logs/run_32b_ft_then_qwen3_0p6b_distill_launcher.log 2>&1 < /dev/null &`
  - New launcher PID on `gpu009`: `1304748`
  - Relaunch verified from shared log: it is currently running CPU-side preprocessing (`score_answers`) on 32B own-data raw logs; sample observed around `352/498` evaluated. GPUs are expectedly idle during this stage.
- Operational rule reinforced:
  - Before each new task on a node, clear prior compute resources first (`run_experiment`, `serve_vllm.py`, `vllm serve`, `torchrun`, `finetune_sft.py`, launchers), then start the next stage.

---

- Time: 2026-03-30 10:xx EDT
- Updated by: codex-木守宫
- Context: Previous Hopper cards expired. User requested a fresh 12h 4xA100 reservation and a check on whether yesterday's unattended experiments produced normal outputs.
- New paired reservation:
  - `6635429` on `gpuq` = `PD`
  - `6635430` on `contrib-gpuq` = `PD`
  - Request method: cloned known-good full-node 4h hold scripts under `/scratch/wzhao20/llama_factory/slurm/trl`, changed walltime to `12:00:00`, then `sbatch`.
- Yesterday's output audit:
  - 32B own-data FT finished successfully for all three lambdas; present directories:
    - `agent_baseline_2epochs_math32b_entropy_owntraj_ds_lambda0p2`
    - `agent_baseline_2epochs_math32b_entropy_owntraj_ds_lambda0p5`
    - `agent_baseline_2epochs_math32b_entropy_owntraj_ds_lambda0p8`
  - 32B-teacher -> 0.6B student distill produced:
    - complete-looking outputs for `math32b_base_singletraj_basicdistill`
    - complete-looking outputs for `math32b_lambda0p2_singletraj_basicdistill`
    - complete-looking outputs for `math32b_lambda0p5_singletraj_basicdistill`
    - incomplete output for `math32b_lambda0p8_singletraj_basicdistill` (only `training_args.json` observed)
  - 14B-teacher -> 0.6B student distill produced:
    - complete-looking output for `math14b_base_singletraj_basicdistill`
    - no completed lambda-based 14B student outputs observed
    - `run_qwen3_0p6b_from_14b_teachers_math_python_launcher.log` is stuck at `Processing questions: 499/500`, so the chain likely died during FT-14B(`lambda=0.2`) teacher recollection and never progressed further.
  - `run_32b_ft_then_qwen3_0p6b_distill_launcher.log` reached the student-train handoff for FT-32B(`lambda=0.8`) with filtering summary `raw=500 scored=499 filtered=326`, but the student output directory did not finish materializing before cards expired.
- Practical interpretation:
  - Yesterday's 32B FT itself is good.
  - The 32B student distill batch is partially successful (`base`, `0.2`, `0.5` done; `0.8` incomplete).
  - The 14B student distill batch is mostly not complete (only `base` done).

---

- Time: 2026-03-30 12:58 EDT
- Updated by: codex-木守宫
- Context: User asked to use one of the newly arrived card groups to continue the unfinished experiments and asked whether mid-experiment local Python edits would affect running Hopper jobs.
- Code-modification boundary clarified:
  - Local workspace Python edits do not affect already running Hopper jobs unless the changed files are synced to `/scratch/wzhao20/AgentDistill` and then picked up by newly started remote processes.
  - Already running Python processes usually keep executing with code already loaded in memory; later stages of unattended chains can change behavior if remote files are edited before those later stages start.
- Current reservation state:
  - `6635430` (`contrib-gpuq`) is `RUNNING` on `gpu009`
  - `6635429` (`gpuq`) remains pending
- Continuation action:
  - Added a targeted resume script to continue only unfinished work:
    - `/scratch/wzhao20/AgentDistill/scripts/training/resume_qwen3_0p6b_pending_teachers_math_python.sh`
    - launcher: `/scratch/wzhao20/AgentDistill/scripts/training/launch_resume_pending_teacher_distill_batch.sh`
  - This resume chain skips completed outputs, then continues:
    - 14B teacher -> 0.6B student for `lambda=0.2/0.5/0.8`
    - 32B teacher -> 0.6B student for any missing variant (currently `lambda=0.8`)
    - final student evaluation
- Live status on `gpu009`:
  - Resume script process is active.
  - It has already skipped completed `14B base` student output.
  - Current stage is FT-14B(`lambda=0.2`) teacher recollection:
    - active processes include `collect_teacher_math_python_singletraj.sh`, `serve_vllm.py`, and `vllm serve Qwen/Qwen3-14B` with the `lambda0p2` LoRA.

---

- Time: 2026-03-30 (later) EDT
- Updated by: codex-木守宫
- Path policy update:
  - Goal is to stop treating `/scratch/wzhao20/AgentDistill` as the canonical Hopper source tree.
  - New canonical remote path should be:
    - `/scratch/wzhao20/AKDA2`
    - with AgentDistill launched from `/scratch/wzhao20/AKDA2/AgentDistill`
  - Intended benefit: future sync can happen through the single `AKDA2` git repo instead of per-file ad-hoc copies.
  - Old path `/scratch/wzhao20/AgentDistill` should be treated as legacy/runtime residue during migration only, not as the default place to start new experiments.
- Migration step requested by user:
  - Stop current `gpu009` FT-14B(`lambda=0.2`) teacher recollection.
  - Stand up `/scratch/wzhao20/AKDA2` on Hopper from the current local workspace state.
  - After path migration, run a minimal smoke experiment from the new `AKDA2/AgentDistill` path to verify the unified layout works.

- Migration validation result:
  - `gpu009` old recollection task was stopped and GPUs were returned to idle.
  - `/scratch/wzhao20/AKDA2` now exists on Hopper with `.git`, `AgentDistill`, and `workbench`.
  - Minimal smoke test was run from `/scratch/wzhao20/AKDA2/AgentDistill` using the real wrapper entrypoint `scripts/training/train_student_from_single_teacher_math_python.sh`.
  - Important runtime requirements confirmed for the unified path:
    - must use Hopper conda env path `/scratch/wzhao20/conda_envs/llama-factory311-clean/bin`
    - must set `PYTHONPATH=/scratch/wzhao20/AKDA2/AgentDistill/src:${PYTHONPATH:-}` so local `src/smolagents` is used

---

- Time: 2026-03-31 12:20 EDT
- Updated by: codex-木守宫
- Context: User requested progress checks on the active `gpu022` old-version 14B recollection lane and the `gpu024` 32B-student evaluation lane, with an instruction to diagnose and repair any non-normal stop instead of only reporting status.
- Findings:
  - `gpu022` old-version `14B lambda=0.2 seed42` recollection had completed raw collection and scoring but stopped before `filter`.
    - raw: `500`
    - scored: `499`
    - score result: `342/499 = 68.54%`
  - This stop was not a normal terminal state for the one-task recollection lane because the user expects a full `raw -> scored -> filtered` handoff before resources are cleared.
  - `gpu024` did not actually stop early; the earlier status check used the wrong result filename pattern.
    - All six evaluation outputs for the three 32B-derived students are present:
      - MATH and GSM-hard for `base`
      - MATH and GSM-hard for `lambda0.2`
      - MATH and GSM-hard for `lambda0.5`
    - Confirmed accuracies:
      - `math32b_base_singletraj_basicdistill`: `MATH 347/500 = 69.4%`, `GSM-hard 229/500 = 45.8%`
      - `math32b_lambda0p2_singletraj_basicdistill`: `MATH 316/500 = 63.2%`, `GSM-hard 240/500 = 48.0%`
      - `math32b_lambda0p5_singletraj_basicdistill`: `MATH 330/499 = 66.13%`, `GSM-hard 243/500 = 48.6%`
- Repairs applied:
  - On `gpu022`, manually ran `filter_agent_training_data` for the old-version `14B lambda=0.2 seed42` scored file.
    - Result: `filtered=342`, `empty logs=0`
  - After that, resources were cleared and the next single task was launched, per user instruction:
    - `14B lambda=0.5 seed42` old-version recollection on `6635429@gpu022`
    - active launcher log: `/scratch/wzhao20/AgentDistill/logs/oldcollect_qwen3_14b_lambda0p5_seed42_launcher.log`
- Next step:
  - Continue monitoring `gpu022` until `14B lambda=0.5` completes `raw -> scored -> filtered`, then clear resources and launch `14B lambda=0.8`.
  - `gpu024` no longer needs repair for the current 32B-student evaluation request because all three student models now have both `MATH` and `GSM-hard` scored outputs.
  - Smoke test outcome:
    - `score_answers` and `filter_agent_training_data` completed successfully from the new path
    - wrapper entered `torchrun + DeepSpeed`
    - tokenization completed
    - training loop reached live steps (`0/19` then `1/19`)
    - run terminated only because it was intentionally wrapped in `timeout`
  - Additional note discovered during smoke:
    - `DATASET_SIZE > 0` currently breaks because `finetune_sft.py` slices a Hugging Face `Dataset` with `train_dataset[:N]`, which returns a `dict`, causing TRL `SFTTrainer` to fail
    - production-style `DATASET_SIZE=-1` avoids this issue and was used for the successful smoke validation
  - Post-smoke cleanup complete: `gpu009` GPUs back to `1 MiB`, `0%`.

---

- Time: 2026-03-30 (later again) EDT
- Updated by: codex-木守宫
- Follow-up on the resumed `gpu009` teacher-distill chain from the unified path:
  - The first post-migration relaunch again appeared stuck around FT-14B(`lambda=0.2`) teacher recollection.
  - Root cause was not a new-path import/runtime failure; the chain was reusing historical launcher content and the single-teacher recollection step was still vulnerable to the same near-complete stall pattern.
  - `scripts/inference/collect_teacher_math_python_singletraj.sh` was updated to mirror the earlier near-complete recovery logic used for batched collection:
    - poll output line count
    - detect stalls near completion
    - terminate the stuck process group
    - retry up to a bounded number of attempts
    - also force-complete if the expected line count is reached but the run process never exits
- Recovery status:
  - Old `gpu009` vLLM / `run_experiment` processes were cleared.
  - The resume chain was relaunched from `/scratch/wzhao20/AKDA2/AgentDistill`.
  - Current FT-14B(`lambda=0.2`) recollection is now genuinely progressing:
    - output file under `/scratch/wzhao20/AgentDistill/training_outputs/qwen3-14B/.../qa_results/...seed42.jsonl` is being rewritten
    - observed line count resumed growth (`61`, then `81`)
    - `gpu009` returned to active decode load (`95%+` GPU util on all four cards)
  - Conclusion: the blocked chain was successfully unstuck and is again moving through the FT-teacher recollection stage under the unified AKDA2 launch path.
- 2026-03-31 update (`codex-木守宫`):
  - User updated `AgentDistill/exps_research/unified_framework/processors/base.py` (`process_dataset`) locally.
  - Verified local diff adds `per_task_timeout` handling plus non-blocking `ProcessPoolExecutor.shutdown(wait=False, cancel_futures=True)`.
  - User then clarified the intended local sync set also includes:
    - `/Users/shimu/Downloads/DOGe-main/AgentDistill/exps_research/unified_framework/processors/agent.py`
    - `/Users/shimu/Downloads/DOGe-main/AgentDistill/src/smolagents/models.py`
  - Synced all three updated files to Hopper runtime path:
    - `/scratch/wzhao20/AKDA2/AgentDistill/exps_research/unified_framework/processors/base.py`
    - `/scratch/wzhao20/AKDA2/AgentDistill/exps_research/unified_framework/processors/agent.py`
    - `/scratch/wzhao20/AKDA2/AgentDistill/src/smolagents/models.py`
  - Relaunched pending teacher-distill recovery chain on active reservation `6658252@gpu008`.
  - Current launcher:
    - `/scratch/wzhao20/AgentDistill/logs/resume_qwen3_0p6b_pending_teachers_math_python_launcher.log`
  - Immediate post-relaunch status:
    - launcher shell alive: `bash scripts/training/resume_qwen3_0p6b_pending_teachers_math_python.sh`
    - child collector alive: `bash scripts/inference/collect_teacher_math_python_singletraj.sh`
    - GPUs showed fresh activity (`gpu2 ~100%`, `gpu3 ~79%`) at the immediate poll, indicating the new code path had begun progressing beyond plain shell startup
- 2026-03-31 later update (`codex-木守宫`):
  - User asked to use `6635429` to continue collecting `32B` teacher variants.
  - Verified reservation status:
    - `6635429 gpuq R gpu022`
  - Verified current `seed42` teacher raw coverage:
    - `32B lambda0.2`: `500`
    - `32B lambda0.5`: `500`
    - `32B lambda0.8`: `500`
    - `32B base` at the path actually consumed by the distill pipeline:
      - `/scratch/wzhao20/AgentDistill/logs/qa_results_python_only_teacher/math_500_20250414_test/Qwen3-32B_temp=0.7_seed=42_type=agent_steps=5_python_only_python_only_seed42.jsonl`
      - only `499`
  - Therefore only the missing `32B base seed42` teacher collection was relaunched on `gpu022`; completed FT variants were left untouched.
  - Relaunch command path:
    - `/scratch/wzhao20/AKDA2/AgentDistill/scripts/inference/collect_teacher_math_python_singletraj.sh`
  - Launcher log:
    - `/scratch/wzhao20/AgentDistill/logs/collect_qwen3_32b_base_seed42_launcher.log`
  - Immediate post-launch status:
    - shell alive: `bash scripts/inference/collect_teacher_math_python_singletraj.sh`
    - `serve_vllm.py` + `vllm serve Qwen/Qwen3-32B` alive
    - GPUs still idle at first poll, indicating early server startup rather than active decoding yet
- 2026-03-31 14B-student update (`codex-木守宫`):
  - User asked to use `6635426` to train students from already-collected `14B` teacher data if complete.
  - Verified `6635426 gpuq R gpu024`.
  - Verified teacher raw coverage used for student distillation:
    - base `14B` raw path under `/scratch/wzhao20/AgentDistill/logs/qa_results_python_only_teacher/...seed42...jsonl`: `501`
    - `14B lambda0.2` FT raw: `500`
    - `14B lambda0.5` FT raw: `500`
    - `14B lambda0.8` FT raw: `500`
  - Verified FT filtered files already exist:
    - `lambda0.2`: `116`
    - `lambda0.5`: `129`
    - `lambda0.8`: `113`
  - Instead of recollecting teachers, created a node-local batch script on `gpu024`:
    - `/tmp/train_14b_students_body.sh`
  - This script sequentially runs `train_student_from_single_teacher_math_python.sh` for:
    - `math14b_base_singletraj_basicdistill`
    - `math14b_lambda0p2_singletraj_basicdistill`
    - `math14b_lambda0p5_singletraj_basicdistill`
    - `math14b_lambda0p8_singletraj_basicdistill`
  - Batch launcher:
    - `/scratch/wzhao20/AgentDistill/logs/train_qwen3_0p6b_from_14b_existing_teachers_launcher.log`
  - Immediate status after launch:
    - launcher shell alive: `bash /tmp/train_14b_students_body.sh`
    - GPUs were still idle at the first poll, so this was still in early shell/script setup rather than confirmed torchrun yet
- 2026-03-31 32B lambda0.8 reset (`codex-木守宫`):
  - User requested a no-code-change retry of the suspicious `32B lambda0.8` singletraj teacher collection.
  - On `6635429@gpu022`, deleted the existing `seed42` trio:
    - raw
    - scored
    - filtered
  - Relaunched fresh collection only for:
    - `MODEL_ID=Qwen/Qwen3-32B`
    - `LORA_FOLDER=/scratch/wzhao20/AgentDistill/training_outputs/qwen3-32B/agent_baseline_2epochs_math32b_entropy_owntraj_ds_lambda0p8`
    - `SEED=42`
    - `N=1`
  - Launcher log:
    - `/scratch/wzhao20/AgentDistill/logs/recollect_qwen3_32b_lambda0p8_seed42_launcher.log`
  - Immediate post-launch status:
    - shell alive: `bash scripts/inference/collect_teacher_math_python_singletraj.sh`
    - `serve_vllm.py` + `vllm serve Qwen/Qwen3-32B` alive
    - GPUs still idle on first poll, indicating early vLLM startup rather than active decode yet
- 2026-03-31 gpu024 repurpose (`codex-木守宫`):
  - User decided the current `14B` student outputs are invalid because the underlying teacher data are suspect.
  - On `gpu024`, stopped the active `14B` student batch and deleted these student output directories:
    - `/scratch/wzhao20/AgentDistill/training_outputs/qwen3-0.6B/agent_baseline_2epochs_math14b_base_singletraj_basicdistill`
    - `/scratch/wzhao20/AgentDistill/training_outputs/qwen3-0.6B/agent_baseline_2epochs_math14b_lambda0p2_singletraj_basicdistill`
    - `/scratch/wzhao20/AgentDistill/training_outputs/qwen3-0.6B/agent_baseline_2epochs_math14b_lambda0p5_singletraj_basicdistill`
    - `/scratch/wzhao20/AgentDistill/training_outputs/qwen3-0.6B/agent_baseline_2epochs_math14b_lambda0p8_singletraj_basicdistill`
  - Reused the same node for clean `32B` student training, but only for teacher variants that currently look acceptable:
    - `math32b_base_singletraj_basicdistill`
    - `math32b_lambda0p2_singletraj_basicdistill`
    - `math32b_lambda0p5_singletraj_basicdistill`
  - Node-local batch script:
    - `/tmp/train_32b_students_body.sh`
  - Launcher log:
    - `/scratch/wzhao20/AgentDistill/logs/train_qwen3_0p6b_from_32b_clean_teachers_launcher.log`
  - Immediate status after launch:
    - launcher shell alive: `bash /tmp/train_32b_students_body.sh`
    - GPUs still idle at first poll, so the batch had entered early shell setup but torchrun was not yet confirmed at that instant
- 2026-03-31 local repo unification (`codex-木守宫`):
  - User requested a real single-folder, single-repo structure so local and Hopper can be synced through `AKDA2` git.
  - Removed nested local git metadata:
    - `/Users/shimu/Downloads/DOGe-main/AgentDistill/.git`
    - `/Users/shimu/Downloads/DOGe-main/LlamaFactory-main/.git`
  - Verified that `git rev-parse --show-toplevel` from inside local `AgentDistill` now resolves to:
    - `/Users/shimu/Downloads/DOGe-main`
  - Verified the only remaining local `.git` under the workspace is the outer root:
    - `/Users/shimu/Downloads/DOGe-main/.git`
  - Practical meaning:
    - Future local `git add/commit/push` should happen from the outer `AKDA2` repo only.
    - Hopper-side canonical git path remains `/scratch/wzhao20/AKDA2`, with experiments launched from `/scratch/wzhao20/AKDA2/AgentDistill`.
    - `AgentDistill` and `LlamaFactory-main` are now ordinary subdirectories from the local git perspective and can be tracked by the outer repo.
- 2026-03-31 Hopper repo unification sync (`codex-木守宫`):
  - User asked to verify Hopper-side changes before any unified merge.
  - Verified by SHA256 that Hopper `/scratch/wzhao20/AKDA2` matches local for:
    - `AgentDistill/exps_research/finetune_sft.py`
    - `AgentDistill/exps_research/unified_framework/processors/agent.py`
    - `AgentDistill/exps_research/unified_framework/processors/base.py`
    - `AgentDistill/scripts/inference/run_agent_python_only_data_sequence.sh`
    - `AgentDistill/src/smolagents/models.py`
    - modified `LlamaFactory-main` files under `research_platform_trl/` and `slurm/`
  - Resynced local `workbench` to Hopper and verified by SHA256 that the following now match:
    - `workbench/AI_REGISTRY.md`
    - `workbench/DATA_REGISTRY.md`
    - `workbench/HUB.md`
    - `workbench/current/active_tasks.md`
    - `workbench/current/latest_handoff.md`
    - `workbench/tasks/2026-03-30.md`
    - `workbench/tasks/2026-03-31.md`
  - Removed nested Hopper git metadata:
    - `/scratch/wzhao20/AKDA2/AgentDistill/.git`
    - `/scratch/wzhao20/AKDA2/LlamaFactory-main/.git`
  - Cleaned Hopper AppleDouble junk files under `/scratch/wzhao20/AKDA2`
  - Verified Hopper now has only one git root:
    - `/scratch/wzhao20/AKDA2/.git`
- 2026-03-31 outer-repo commit (`codex-木守宫`):
  - Added `AgentDistill`, `LlamaFactory-main`, and `workbench` into the outer local repo and created commit:
    - `788a688` `unify AgentDistill, LlamaFactory-main, and workbench under AKDA2`
  - Pushed `main` to `origin`.
  - GitHub responded that the repository has moved and the new preferred URL is:
    - `https://github.com/shimu0215/DOGe.git`
  - Current practical state:
    - local outer repo has the unification commit
    - Hopper outer repo content is already synced by file content, but because Hopper still has those directories as untracked files in its working tree, a clean git-based Hopper pull/checkout step still needs to be done carefully before relying on pure `git pull` there
