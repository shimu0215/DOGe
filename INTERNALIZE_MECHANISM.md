# 最简固化候选：替换最后一层已有的单个 SwiGLU 神经元

本文件描述已实现的 existing_neuron 模型。**它尚未证明有稳定的 OPD 防护效果**；完整对照与其他方法见 [实验报告](/Users/shimu/Downloads/opd-gate-audit-20260909/INTERNALIZE_REPORT.md)。

## 模型形式

原 MLP 输出为 Σ_j d_j SiLU(g_jᵀx)(u_jᵀx)。只替换一个现有神经元，因此新的 MLP 与原 MLP 的差值严格为：

ΔMLP(x) = d_new a_new(x) − d_old a_old(x)。

这不是在模型旁边增加一个分类器或新增一条残差分支；原神经元的贡献同时被移除。推理架构和所有矩阵形状不变。

训练阶段用冻结原 teacher 的隐藏输入 x 拟合：

- w：成对 teacher / proxy student 训练上下文的来源方向。
- c：岭回归得到的近似常数方向，cᵀx≈1。
- τ：在缓存正例上的保守阈值。
- β：控制过渡斜率，本次为 80。

写回已有权重：g_new=β(w−τc)，u_new=c/β。

于是 a_new(x)=SiLU(β(wᵀx−τcᵀx))(cᵀx)/β。输入表现为来源正例时希望激活趋近于零；越过阈值时沿 d_new 干扰后续输出。

d_new 由原 teacher 数字“9”的输出头向量减去十个数字输出头向量的均值，再结合最终 RMSNorm 权重及残差尺度构造。这是一个固定 teacher 来源的输出方向，不是读取学生概率后反向优化出的在线方向。最终 RMSNorm 会产生非线性变化，因此不能把残差方向简单称为精确的指定 logit 增量。

## 可证明的部分和不能证明的部分

对 t≤−m，SiLU(t)=t/(1+exp(−t))，因此 |SiLU(t)|≤|t|exp(t)。如果同时控制 |cᵀx| 和 t 的负间隔，可以约束新增神经元激活。这是针对给定输入的解析界。

总扰动还包含被删除的旧神经元贡献。后续归一化、softmax 和自回归生成均可能放大扰动。因此本式不证明 teacher 准确率不下降；它也不证明负例干扰足以改变 OPD 学习动态。两者必须独立评测。

在训练缓存上选择旧贡献较小的单元 18356，共写入三个 3,584 维向量。校准正例包含来源分类验证题的正例，所以极小校准激活不是未见正例的误触发率保证。分类验证学生负例只有约 1.43% 的位置激活超过 0.01；低覆盖可能解释弱抑制，尚未通过消融验证。

## 训练与部署的边界

训练需要已知 proxy/student 负例，及原 teacher 正例；这是用户允许的训练信息。训练用 hook 只收集隐藏表示，保存前全部移除。部署只需原架构的普通 Qwen checkpoint，不加载来源向量文件、外部 gate、proxy、adapter 或自定义生成器。

这是一种拟合后直接写回权重的受约束后训练。它证明了“条件行为可以用现有末层权重表达”的工程可行性；当前学生实验仅有小幅、不显著的下降，不能把结构可行性当成目标已达成。
