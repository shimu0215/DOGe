# Live-proxy learning-direction pilot, fixed before launch

User authorized two existing GPUs on Sep10 evening. Use9795227/gpu008 until Sep11 02:03 ET and9801342/gpu010 until Sep11 05:11:13 ET; do not reserve or extend jobs. The original RL pilot and queued signal audit finish unchanged on9795227 first.

## What is useful guidance?

The audited MiniLLM implementation uses teacher-minus-student observed-token log probability for rewards, discounted length-normalized/whitened future returns for PPO, and an additional exact conditional reverse-KL gradient. A sparse score change can therefore affect earlier policy updates too. The final teacher answer is not the sole signal. See original [MiniLLM paper](https://arxiv.org/abs/2306.08543) and the local unchanged minillm/losses.py.

Define useful one-step learning as reduction of a proxy correct-answer cross-entropy Q after a distillation update. For proxy parameters phi, distillation loss D, and small SGD step eta:

Q(phi-eta grad D) = Q(phi) - eta <grad Q, grad D> + O(eta^2).

Minimizing this alignment removes the first-order improvement. This is a local Taylor argument, not proof of lower generation accuracy after long OPD. The proxy answer CE is conditioned on its own freshly generated process prefix, followed by a fixed final-answer cue. Only ground-truth numeric answer tokens are scored, excluding formatting/EOS. This remains a conditional likelihood surrogate, not probability of correctness integrated over all CoTs.

## Implemented candidate

Both arms start original7B and original0.5B SFT proxy. Teacher last4layers LoRA rank16/alpha32. Proxy last4layers LoRA rank8/alpha16, FP32 for numerical finite differences. Each iteration generates a fresh proxy response, computes numeric-answer-gradient direction from its own freshly generated process prefix, updates teacher, and actually updates live proxy by clipped SGD(lr0.02) on reverse-KL at24 sampled positions of the complete proxy response. The next iteration generates using that updated proxy. This inner learner does not reproduce full MiniLLM PPO/Adam/full-weight updates. Full audited OPD120 is the external efficacy endpoint.

Let h be normalized proxy answer-loss gradient. Central finite differences at phi +/-0.05h produce c=(p_plus-p_minus)/(2epsilon) and e=d sum(p log p)/dh. Conditional KL gradient alignment is e-sum(c log p_teacher). Teacher gradients through this expression avoid retaining a full student-update second-order graph. CPU tests compare the exact mixed derivative; firstGPUiteration compares epsilon and half-epsilon, requiring cosine>0.9 and relative difference<0.5. Proxy weights restore exactly after perturbation.

Sample24 eligible middle response positions (exclude first32tokens, special tokens and answer region), select top4 by current normalized positive learning alignment. Minimize hinge(alignment/sum(abs(c))+0.02) there. Weight is zero first16updates, ramps to2.0 at64, stays2.0 through128. Sparse selection and proxy are training-only; no special tag, runtime gate, proxy, or adapter in exported teacher. Internal weight changes can affect other positions; sparse training masks do not guarantee sparse inference changes.

Both arms also receive on-policy group4 answer-correctness RL (same reward as previous pilot), reference sampled KL weight0.02, supervised answer CE weight0.5, averaging verified-correct teacher-trace answer-region CE and correct-numeric-answer CE on live-proxy process prefixes, and exact original-teacher trajectory KL weight1 at24 positions. These are preservation objectives, not guarantees. Final answer region starts at the boxed marker and is capped at48tokens.

Arms: directional (anti weight2) on9801342; preservation_control (anti weight0) on9795227 after originalqueue and signal audit finish. Both128updates, teacherlr1e-5, seed1010, teacherrawgeneration max512, freshproxy max384. Same original320train/64validation prompt partition; eligible TRAIN questions must have a correct teacher trace. Test sets never used for teacher/proxy optimization. No checkpoint selection using student test results. All losses and finite-difference diagnostics recorded.

## Evaluation and limits

Merged ordinary teacher: greedy, ordinary sampling and raw sampling first64, then200 and reused GSM600:800 as time permits. Full unchanged OPD120 starts from the original full-weight SFT student, seed10, with original-teacher same-seed and preservation_control comparisons. Previously observed original-teacher OPD gains are unstable, so matching SFT alone is not defense evidence. Report paired uncertainty and preservation failures. Additional seeds or distinct students only after primary matched comparison and within existing time.

This is hybrid outcome RL plus a one-step learning-direction meta objective, not black-box RL rewarded by a full student training run. Closed-loop proxies are used for training only. The sparse meta objective initially covers direct reverse KL; long-horizon PPO reward effects are tested by actual OPD, not differentiated through in this pilot. The approach is motivated by differentiable teaching, e.g. [Learning to Teach with Dynamic Loss Functions](https://arxiv.org/abs/1810.12081), adapted here to remove rather than improve first-order answer learning.

## Pre-formal diagnostic correction at21:44ET
The first successful2-step smoke revealed proxy answer CE around1e-5 on correct teacher traces: the teacher derivation already contained the correct numeric result, making the target near-trivial copying. Before any formalarm began, proxy learning measurement changed to numeric-answer-only CE on the live-proxy process prefix. Teacher preservation now includes this live-prefix numeric target too, to encourage recovery from imperfect reasoning. Original successful smoke is archived as smoke.teacher_trace_target; failed initialFP16smoke as smoke.fp16_overflow. Repeat both-update/merge checks with corrected target before formal launch. Gradient scale128 resolves observed default65536 FP16overflow while keeping nonfinitefailures fatal.

## Third GPU / independent strong-target arm, added21:49ET
User authorized a third existing GPU:9801341/gpu019 until2026-09-11 05:42:19ET. Keep it; do not cancel it as a redundant reservation. No new/extended reservations.

Strong_rank uses a separate train_strong.py copied from corrected live-prefix training, leaving running train.py unchanged. Same128updates, schedule, LoRA scope, outcomeRL/answerCE/own-trajectoryKL, live-proxygeneration and proxySGD as otherarms. Replace sparse learning-alignment objective with forwardKL to top32-reversed ORIGINAL teacher probabilities. Select4of24middlepositions with largest original-to-reversed KL; skip positions withEOS/pad amongoriginaltop2. AllspecialIDs andpaddedvocabularyprotected. Targets and positionimportance depend on originalteacher probabilities andfixedmetadata, neverproxylogits/gradients. Liveproxy provides trainingcontexts only; finalteacherplainmergedweights.

Reversal preserves the target probability multiset, partition function and entropy, and maximizes original-to-target KL among permutations of the chosen candidate set by the rearrangement inequality. It does NOT guarantee semantic wrongness, teacher preservation, or negative student learning. Check partition/entropyerrors<1e-5 andexactprotectedscores instrong_smoke, then actualOPD.

The same preservation_control is a matched control for allcommontrainingobjectives. Strong_rank is an alternative mechanism, not a parameter selection of directional. Thirdarmqueue run_strong.py, port30005, smokeoutputstrong_smoke, formaloutputstrong_rank, queue strong_rank_queue.json. Full student outputresults/internalize/loop_strong_rank_s10_*. Same old200+reusedextra600 evaluation and paireduncertainty. Current active train.py/run_pilot.py and sharedMiniLLM files remain unchanged.

## Dynamic resource authorization, Sep10 22:53ET
User requests periodic GPU availability checks and automatic inclusion of all available allocated GPUs. Existing fifteen-minute heartbeat now inventories reservations and active steps; newly awarded idle allocations may be used without re-confirmation, with logged resource/device/deadline mapping and appropriate controls. No new reservations or extensions. Current three-card list is no longer a cap. Pending allocations must not be treated as usable before they start, and physical node GPUs not allocated to this user task are not available. Matching-seed confirmation and process/answer mechanism experiments take priority over unguided parameter searches. Keep monitoring while pending reservations remain rather than stopping at the original third-card deadline.

## Matched-seed replication, fixed Sep10 23:32ET
After each arm completes its full primary queue, repeat actualOPD120 with seeds11 and12 sequentially on its sameallocatedGPU and samefixedplainteacher. No teacher retraining or checkpoint selection. Each has matched originalteacher clean11/12 results for old200 and reusedextra200. Allthreearmsrepeated, not just the lowestfirstseed. This tests trainingrandomness, not crossstudent generalization; comparisonsremainexploratory onpreviouslyusedquestions. Separate repeat_seeds.py waitsprimarycomplete/noactiveSlurmsteps, verifies1GPU4CPU32G/node/deadline and visibledevicefree, recordsresultsandfailures, skips if<4200secondsbeforeOPD or<600beforeextraeval. No activecodechanged. Ports30103directional/30101control/30105strong; records{arm}_repeat_queue.json.

LATEST USER PRIORITY Sep11 00:02 ET: stop multi-seed repeats for candidates whose first seed does not work; prioritize new mechanisms. Allthree repeat drivers3745868/3745869/3745870 SIGTERM and correspondingsteps9801342.11/9795227.24/9801341.9 scancelled (stepsONLY, reservations preserved). Repeatqueue records setstopped=True,phase=stopped_by_user_priority,complete=False; previousstates preserved *_repeat_before_user_stop.json. No complete seed11endpoint atstop; seed12notstarted and CANCELLED. DoNOTresume repeats or treatintentionalstopasfailure. Allprimaryseed10resultscomplete andpreserved. Heartbeatteacherupdatedinplace: no multi-seed until firstseedshowsworthwhilejointoutcome; nextresearchfocus actualnormalizedPPO/processandconditionalKLsignals, rather thanproxyconditionalKL-onlysurrogate. Newmechanismnotyetimplemented/launched. ThreeGPUsbecomingidle; checkdevicesbeforeassigning. Continueavailable/pendingallocationinventory andnewmechanismresearch.
