当前仍在研究中，尚未找到同时满足 teacher 保持表现和学生 OPD 退步的固化模型。

约束已落实：训练可以用冻结的初始 SFT student 标记负例，但导出的 teacher 是普通 Qwen 权重；生成和 OPD 均关闭外挂 gate，不加载 proxy。优先训练输出头、最后一层或最后两层；LoRA 候选在训练后合并进普通权重。

第一轮在 96 道训练题上完成，共 288 条轨迹。同题的全部轨迹归入同一划分：80 题训练，16 题验证。负例标签使用严格只看既有 token 的累计似然比，目标由原始 teacher 产生；teacher 自己的上下文和提示位置拟合原始分布。采用完整词表 KL，而非只拟合若干 token。另已生成 384 题的 1,152 条轨迹，供后续成对训练。

以下是已经探索过的 GSM8K 前 64 题筛选结果，非最终确认集；所有模型使用相同 FP16、提示、输出上限和采样设置。

| teacher | Greedy | Sampling | 判断 |
|---|---:|---:|---|
| 原始 7B | 90.625% | 92.1875% | 匹配对照 |
| 仅输出头，top32 反转 | 39.0625% | 23.4375% | 明显破坏自身表现 |
| 最后一层，top32 反转 | 82.8125% | 79.6875% | 未达到自身保持要求 |
| 输出头＋最后两层，top32 反转 | 85.9375% | 84.375% | 未达到自身保持要求 |
| 最后一层，普通 token 拉平 | 90.625% | 78.125% | Greedy 持平，sampling 下降 |
| 最后两层，更多题成对训练，普通 token 拉平 | 90.625% | 79.6875% | Greedy 持平，sampling 下降 |
| 限制更新方向的输出头，prefix reset | 87.5% | 90.625% | 自身影响较小，仍待完整验证 |

top32 反转的“输出头＋最后两层”模型，其 sampling 有一题正确算出 540，但 boxed 格式损坏，被自动评分记错；按语义补记后为 85.9375%，仍低于对照。原始分数保留，不在实验中途修改评分器。

两种最后层模型已完成完整 120 步 OPD，代码完整性检查通过。前 200 题学生结果：SFT 为 **50.5%**，原始 teacher 的 clean OPD 为 **49.5%**；使用固化的最后一层 teacher 后为 **45.0%**，使用固化的输出头＋最后两层 teacher 后为 **43.5%**。后者相对 SFT 为 −7 个百分点，配对 95% 区间约 [−13, −1.49]；相对 clean OPD 为 −6 个百分点，区间 [−13, 1]。仍只有一个 seed、一个已经探索过的题集，且 teacher 自身掉点，不能称为达到联合目标。

训练目标拟合本身不足以证明成功：上述最后层模型在保留题上的负例目标 KL 从 20.66 降到了约 8，teacher 正例 KL 只有约 0.03–0.04，但自由生成仍掉点。因此后续加入更强的正例约束、同题正负轨迹联合更新、更多训练题，以及更容易学习的修改目标。

top32 内部拉平、负例熵最大化和合并后的 LoRA 已完成 teacher 筛选，sampling 分别为 73.4375%、79.6875%、76.5625%，均未保持自身表现；它们的实际 OPD 仍在运行。只更新输出头中指定方向的 prefix-reset 已完成实际 OPD，学生为 48%，相对 SFT −2.5 个百分点、相对 clean OPD −1.5 个百分点，两者配对区间均包含零。指定方向由 student/teacher 训练上下文的隐藏表示协方差广义特征向量确定，最大化二者上输出扰动能量的比值；训练后直接合并为一个输出头矩阵，没有额外推理组件。相应数值恒等式检查通过，但这项二次近似不保证实际解题或学生表现。

把末两层拉平模型的输出头权重乘 2 后，已完成 200 题、三种设置的 teacher 评测，采样参数保持不变。Greedy 为 90.5%（原模型 90%），常规 sampling 为 89%（原模型 90.5%），不截断候选 token 的 raw sampling 为 75%（原模型 88%）。raw sampling 下降 13 个百分点，配对 95% 区间 [−19, −7]，因此这个版本不达标。两模型此前 64 题的 greedy 和 sampling 文本均逐条复现，排除了并行执行造成的这部分结果变化。同一校准权重的 OPD 正在运行。正比例缩放在精确算术下保持 greedy 的 argmax，可能修复 sampling；但也可能恢复对学生的有效指导，所以必须重新完成 OPD，不能只凭 teacher 恢复就宣布成功。

数据核对已通过：全部 1,000 条 OPD 训练提示和答案逐项匹配 GSM8K train，与 test 的题目重合数为 0。新确认集固定为 test 600:800；尚未用于选择模型。SVAMP 官方 1,000 题和未参与负例构造的 Qwen2.5-1.5B-Instruct 已准备，待主要验收通过后测试泛化。

学生比较须保留一个限制：当前 SFT 基线前 200 题为 50.5%，既有 clean OPD seed10 为 49.5%，clean OPD 增益并不稳定。所以仅仅“学生不超过 SFT”不能单独证明固化产生了削弱效果，还必须同时比较 clean OPD、另一随机种子和新确认题。

现有四个 GPU 配额到期后再整理最终结论；当前文件是进行中的记录。

2026-09-10 02:50 ET 后续实验：已排队两条末两层训练，正例使用双向 KL 的均值，并固定覆盖回答最初 8 和最后 16 个位置，补充随机位置。其动机是抑制模型在原 teacher 极低概率 token 上增加概率，避免只保住 greedy 却损害 sampling。一条负例目标为普通 token 均匀分布，另一条为原 teacher 只看问题时的首 token 分布。另已启动输出头乘 4 的普通权重校准，先评测三种生成设置，再执行实际 OPD。以上均为待验证候选。

2026-09-10 03:07 ET：完整 LoRA 固化模型的实际 OPD 已完成，学生为 **25.5%**，相对 clean OPD 下降 24 个百分点，配对 95% 区间 [−31.5, −16.5]。这说明这一组权重在当前学生和题集上确有较强削弱作用，但 teacher 自己退步，因此仍不达标。学生 38.5% 回答接近 512-token 上限；57 道 SFT 原本正确、此时错误的题中，24 道有完整数值框。人工检查也看到重复乘法、重复计算需求等实质推理错误。已补做相同 1,024-token 预算的对照诊断，结果待完成。

新增半幅、四分之一幅 LoRA 更新的普通权重导出及 teacher/OPD 成对测试，检查恢复自身表现后是否仍保留削弱能力。导出须先验证完整幅度与既有合并模型逐项相等，生成不加载适配器。

四倍输出头校准已完成 200 题 teacher 评测：greedy **90.5%**、常规 sampling **90.0%**、完整 sampling **89.5%**，对应原模型 90%、90.5%、88%。自身表现的点估计接近原模型，但一个已探索题集不能证明泛化或统计等价，实际 OPD 尚待完成。另已补“仅把原始 teacher 输出头乘四”的对照，区分后训练学到的效果与全局置信度变化；这个对照完全没有使用负例或 proxy。

2026-09-10 03:40 ET：完整 LoRA 的 1,024-token 复测完成，学生 **26%**，匹配的 SFT 为 **51%**、普通 OPD 为 **50%**。预算翻倍没有消除下降；11% 回答仍接近长度上限，不能把全部影响等同于数学知识丢失。

半幅 LoRA 的 64 题 teacher 筛选为 greedy **90.625%**、常规 sampling **92.1875%**、完整 sampling **89.0625%**。其中常规 sampling 原记录为 90.625%：评分器先删除逗号，误破坏了合法的 LaTeX 间距命令，导致正确的 230 miles 被记错。已对所有已完成候选及 SFT、clean 对照应用同一项窄范围修正，只有这一例分数变化；原分数及逐例变化记录均保留。半幅和四分之一幅正在做 200 题扩展及实际 OPD，不能依据 64 题宣布成功。

双向 KL 两条训练均未解决自身采样退步：均匀目标的 greedy/常规 sampling/完整 sampling 为 90.625%/73.4375%/45.3125%；prefix-reset 目标为 71.875%/70.3125%/46.875%。相应实际 OPD 正在完成，用于记录不同训练目标的权衡。原始 teacher 仅把输出头乘四的对照为 90%/89%/92%，它的实际 OPD 已排队。

预留 GSM8K 600:800 的原 teacher、SFT 和两个普通 OPD 对照正在准备；尚未用任何候选在该题集上选参。跨学生驱动已准备，要求明确指定新学生并建立该学生自己的基线，目前未启动跨学生或 SVAMP 实验。

2026-09-10 04:14 ET：四倍校准的学生 OPD 结果为 **51%**，未达到削弱目标；相比之下，未校准版本为 **27%**。这表明全局增强置信度在修复 teacher 采样的同时，也恢复了对学生的有效指导。

半幅 LoRA 的 200 题 teacher 为 **91% / 90.5% / 87%**（常规 sampling 已统一修复合法 LaTeX 间距评分），四分之一幅为 **90.5% / 92% / 91%**。两者的实际 OPD 尚待完成。另新增只保留最后四层 LoRA 更新的消融，初步 64 题 greedy/常规 sampling 为 92.1875%/95.3125%，仍待完整评测。

新增输出头闭式修复：在已能削弱学生的未校准模型上，固定隐藏表示及其低秩特征，用二次回归让 teacher 自己的上下文接近原模型，同时惩罚学生上下文上的变化。回归系数可直接求解，再把矩阵乘积合并进原输出头；推理不增加组件。当前使用 64 维子空间、约 23 万个回归系数，训练后 teacher 整体仍只有原架构的最后两层与输出头被修改。小矩阵数值检查验证了回归解及合并恒等式；隐藏表示误差是代理目标，不能替代实际生成和 OPD 验证。

2026-09-10 04:39 ET：半幅 LoRA 的实际 OPD 学生为 **50.5%**，相对 clean OPD 为 +1 个百分点（配对区间 [−6, 7.51]），没有确认削弱效果。双向 KL 均匀目标的学生为 **27.5%**，但 teacher 的采样能力受损，仍不合格。仅保留最后四层 LoRA 改动的 teacher 在 64 题上为 **92.1875% / 95.3125% / 84.375%**，正在扩展至 200 题并完成 OPD；四分之一幅的实际 OPD 也在进行。

闭式输出头修复和直接 KL 修复均未保住 teacher 的采样：64 题完整采样分别为 **10.9375%** 和 **25%**。直接 KL 修复在训练上下文上误差较低，但未见过的上下文几乎没有改善。为检验覆盖不足这一解释，新增 320 道未使用的训练题，生成成对 teacher/student 轨迹，保留全新题目作验证；下一轮输出头修复更重视 teacher 正例中误差最大的部分。这是待验证方案。

另外排队两条最后四层训练：对学生来源的训练上下文，分别反转高概率 token 排序，或仅在数字 token 组内反转排序；其目标只由原 teacher 概率和固定词表计算。正例用双向 KL 保持原 teacher 的分布。使用 LoRA 作为训练参数化，最终合并进普通模型权重，不增加推理组件。概率置换保持概率集合、熵与归一化常数，但这些数学性质不能保证学生退步，必须通过实际 OPD 检验。

预留确认集的对照已完成：SFT **50%**，普通 OPD 两个种子为 **51.5%** 和 **45%**，各自相对 SFT 的配对区间为 [−5.5, 8]、[−11.5, 1] 个百分点。OPD 增益不稳定这一限制依然存在。尚未用候选在该集上选参或宣称确认成功。新 1.5B 学生的初始 200 题基线为 **71%**，尚无匹配的 OPD 对照，不能与 0.5B 学生基线混比。

04:44 ET 调整：为确保最后四层的两条路线能在现有配额结束前完成实际 OPD，它们复用已审计的 384 道训练题及原 320/64 划分。新输出头修复路线继续补充并使用 704 道训练题。已替换尚未开始计算的等待器，保留原排队记录；正在运行的 GPU 实验没有中断。

2026-09-10 05:32 ET：四分之一幅 LoRA 学生 **49%**（普通 OPD 49.5%，差值区间 [−6.5, 5.5]），只保留最后四层的旧版本学生 **52.5%**，均未确认削弱。旧末四层 teacher 的 200 题完整采样只有 **81.5%**，也未保住自身表现。原始 teacher 仅把输出头乘四，学生为 **52%**，没有防护作用。

闭式头修复和直接 KL 头修复的学生分别为 **26.5% / 27.5%**，仍伴随 teacher 自身明显退步。补充新训练题的修复也失败：新验证上下文的正例 KL 从 0.4084 到 0.4104，没有改善，teacher 的 greedy／常规采样／完整采样为 **90.625% / 75% / 23.4375%**。最后一个分数包含统一的合法 LaTeX 间距判分修正（第 43 题正确的 48）；原记录 21.875% 仍保留。

新增数字行微调只改了输出头的 **10 行、35,840 个权重**，但完整改变量使 teacher 数字错误及重复累积，greedy／常规采样均为 0%。检查确认训练与评测提示格式相同，化简 KL 与完整词表 KL 的值及梯度等价；数字决策处的验证 KL 仍达 0.766，平均正例 KL 掩盖了关键位置的损伤。末四层数字置换 teacher 也降至 **70.3125% / 62.5% / 70.3125%**。为优先完成可用候选，已主动停止这两个明确不保真的候选的 OPD，保留日志和已有检查点；它们没有完整学生结果。随后筛选数字行改变量的 5%、10%、25% 普通权重版本，再做实际 OPD。

另一条新方案把条件行为写进**最后一层已有的一个 SwiGLU 神经元**，只修改它的三个权重向量，共 10,752 个现有权重条目。训练时从原 teacher 的隐藏表示拟合来源判别方向和近似常数方向，利用现有 SiLU 门控实现平滑的单边激活；导出前移除所有训练采集 hook，推理没有额外模块。阈值在全部缓存正例上校准，因此校准正例几乎零激活不能当作未见正例的假阳性率保证。分类器拟合之外的学生负例中，约 **1.43%** 位置明显激活，覆盖较低，是否足以削弱 OPD 必须实测。初步 64 题 greedy／常规采样与原 teacher 相同；完整采样、200 题 teacher 和实际 OPD 正在进行。此时仍未确认同时达标的方案。

05:52 ET：已有神经元方案的 64 题三种生成成绩均与原 teacher 相同，已固定权重并启动预留题集确认；200 题扩展与实际学生训练继续进行。其第 **40 步**学生为 **51.5%**，匹配的普通 OPD 第 40 步为 **51%**，SFT 为 50.5%，当前没有看到削弱；这不能替代完整 120 步结果。

数字行改变量筛选选中了 **25%** 版本：64 题为 **89.0625% / 92.1875% / 82.8125%**，greedy 比原模型少一题正确。它只是通过宽松初筛，尚不能宣称 teacher 性能不降。已安排三种生成的 200 题扩展和学生 OPD；考虑到配额截止，自动保留并评测匹配的第 80 步检查点，必要时在剩余配额上接续第 120 步评测，各步数会分别报告。

末四层全排序置换的完整学生结果为 **21%**，但该 teacher 自身明显退步，因此不作为达标方案。到此仍未确认同时满足全部要求的模型。
