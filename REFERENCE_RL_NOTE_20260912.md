# Teacher-reference correctness baseline: experimental rationale

The current group-standardized correctness advantage is zero when every sampled answer receives the same reward. This is expected behavior of group-relative optimization, not an implementation error. Increasing group size may help, but does not guarantee informative groups.

The new prototype samples current-teacher answers and an independent frozen-original-teacher answer group for the same question. It uses A = R(current answer) - mean R(original group), without group whitening. All outcomes use ground-truth final-answer correctness and the existing truncation penalty. No student model, score, gradient or reward enters teacher training.

For a fixed question x and any action-independent baseline b(x), the score-function identity gives E[(R(y)-b(x)) grad log pi(y|x)] = grad E[R(y)]. An independently sampled frozen-reference mean is also an action-independent baseline in expectation. It is a variance-reduction candidate, not guaranteed to minimize variance. A current all-wrong group can receive negative advantages when the reference succeeds. If both groups have the same rewards, the signal may still vanish.

The prototype uses a sum over generated-token policy terms divided by fixed512, rather than dividing each trajectory by its own realized length. At the on-policy evaluation point, where the per-token importance ratios equal1 and clipping is inactive, this yields the sequence score-function gradient up to a fixed factor. This claim does not extend to arbitrary off-policy/PPO updates. We use one gradient update per freshly generated group. Truncation is treated as part of the finite-horizon outcome definition.

This modification preserves an expected correctness-gradient interpretation; it does not prove teacher noninferiority or student OPD suppression. Negative process divergence and own-context anchors remain separate objectives. The actual experiment also changes reward normalization and group sizes relative to the stronger-RL variant, so it is not a single-factor baseline ablation.

Experiment: original7B direct final layer,64steps LR5e-6, bounded divergence T4/c1 anti.5 anchor12 answer2, RL8 every2steps, currentgroup4/referencegroup4, warm16/ramp48. Independent reference generation uses a separate RNG seed and forked RNG state. Math checks cover the finite-action baseline identity, equal-group correction signs, and fixed token normalization; GPU smoke precedes fresh full training. Final teacher is an ordinary checkpoint without a reference model at inference.

## September12 signal audit and prepared own-outcome selection

The completed anchor24 reference-RL formal64 run had32 RL groups, only4 with nonzero mean absolute advantage. Pure self-RL last-two-layer formal64 had16 groups,15 with rewardmean1 and one with.75. These are small observed training samples, not a universal RL limitation. They suggest correctness supervision is often uninformative on the selected easy questions; scaling its weight alone does not create a missing advantage.

A CPU audit of original-teacher sampled/greedy offline outputs on training IDs found39 questions with one failure and one correct available anchor among290 eligible questions. Four new hard-RL prototype files are prepared LOCAL ONLY at this entry, not deployed or launched. They change ONLY the prompt used for on-policy correctness RL to these teacher-outcome disagreement questions, retain the original static positive/negative question order and weights, and log actual RL prompt ID and mean absolute advantage. One uses pure self-RL; one uses the working tail-v2 anti4 recipe. Final-layer/noLoRA, no student outcome or parameter training signal. Each RL question remains within training IDs and has a correct teacher anchor; future runtime asserts ensure that. The heldout64 and evaluation questions are excluded.

This changes the question distribution optimized by RL, so the expected correctness-gradient interpretation is for the reweighted question distribution, not an unbiased estimate for the full original dataset. Original-teacher failure can reflect truncation as well as arithmetic error. More reward variation, preserved own accuracy and reduced student teaching are hypotheses requiring actual experiments. No teacher task reward directly optimizes the student's learning gain.

## September12 13:19 follow-up deployment

The previously local-only pure self-RL and tail-v2 hard-question prototypes are now deployed and launched. A third uses the identical hard-question selection with Top2 anti1.5 control45.5; all three preserve their respective original static loss schedules and change only the RL prompt selection. Actual startup manifests for pureRL andTop2 confirm39 hard training IDs, excluding the heldout64. This is not a student-reward objective. Runtime groups must still be checked for reward variation; difficulty estimated from old teacher samples does not guarantee informative new samples.

Reference-RL anchor24 and pureRL last-two-layer completed teacher checks but both failed old200greedy by.5pp, so no external students were trained for them. Both raw128 were121/128 vsoriginal120, which does not override the main failed screen. PureRL broader student score51.1/1000 is only partial suppression, and its expandedteacher greedy90.5vs91 also fails the point rule. The new hard-question route is an informed experiment, not an established rescue.

At13:40, allthreehard-question formal64 runs completed. Each had16RLgroups; nonzero mean absolute advantages appeared in7 pureRL groups,5 tail groups,4 Top2 groups. Average rewards were.821875/.8875/.9171875 respectively. This verifies more informative groups in the observed runs, especially versus the previous pureRL-last2 run's1/16; parameterizations and some trajectories differ, so it is not a matched pure-RL causal ablation against that last2 run. Student effects remain pending; tail-hardRL old200sampling89vsoriginal90.5 already fails current point threshold.
