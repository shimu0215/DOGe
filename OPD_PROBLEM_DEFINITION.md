# OPD 防御的问题定义与 baseline 研究

2026-09-11。当前判断，随实际实验结果修订。

## 建议的论文主张

主问题建议定义为：在保留 teacher 正常推理能力的条件下，减少攻击者通过学生生成上下文上的 teacher 评分而获得的**额外能力提升**。主要参照是进入 OPD 时的学生，可能是经过 SFT 的学生，也可能是已有能力的基础/指令模型。

将学生压回项目 SFT 前，属于更强的“OPD 导致能力回退”结果，可以作为额外贡献。用户当前授权的探索目标正是这个更强目标；不要把现阶段仅回到 SFT 后起点的结果报告为完成任务。

## SFT 算不算 OPD 的一部分？

区分算法和完整训练配方。OPD 的关键是学生生成序列、teacher 在这些上下文上提供分布/逐 token 信号。SFT 可以是完整配方的初始化阶段，但 SFT 本身不是 on-policy 更新。

- **MiniLLM，ICLR 2024**：第2.3节、Algorithm1及附录B.1明确包含先监督微调学生、按验证损失选择初始化，再进行反向KL蒸馏。论文同时单独比较 SFT baseline。因此可以说其完整流水线包含 SFT，不能把整个流水线的增益全部归因于 OPD。[论文](https://arxiv.org/html/2306.08543v3)、[官方实现](https://github.com/microsoft/LMOps/tree/main/minillm)
- **GKD，ICLR 2024**：第3.1节说明实验从 SFT 学生开始，要求学生能生成足以供 teacher 反馈的序列；第4.1节说明比较方法从相同 SFT checkpoint 开始。GSM8K实验先用 CoT 数据训练，再研究 on-policy 阶段的增益。其算法定义没有要求一个固定长度的 SFT 阶段。[论文](https://arxiv.org/html/2306.13649v3)
- **Thinking Machines，2025技术文章**：主要数学实验先对教师生成数据做 SFT，再做 OPD；Discussion中的计算效率实验则从未追加 SFT 的基础模型开始。它支持“SFT 常见，但不是 OPD 的必要定义”这一判断。该来源是研究团队技术文章，不是同行评议论文。[原文](https://thinkingmachines.ai/blog/on-policy-distillation/)
- **Lightning OPD，2026预印本**：强调 SFT 与 OPD 教师的一致性，尤其是固定离线轨迹近似的条件，并报告不同教师组合的消融。这为检查当前14B→SFT、7B→OPD的来源差异提供了一个假设，但不能据此断言我们更换教师一定无效；GKD也采用大模型生成的 CoT 做初始化，再用另一经过任务微调的教师蒸馏。需要实测，不能把一篇预印本的条件性推导升级为所有在线OPD的普遍定理。[预印本](https://arxiv.org/html/2604.13010v1)

## 为什么主要参照应是 OPD 起点

当前 SFT 数据来自14B，受保护的 teacher 是7B。SFT 阶段的已有知识并不全部来自受保护 teacher。抵消这些已有能力，不能直接等同于阻止从7B获取新知识。

攻击者还可以保留 SFT checkpoint，甚至选择 OPD 过程中表现最好的 checkpoint。设初始学生准确率为 A_init，它可获得的最佳准确率至少为 A_init。因此，让最后一步的模型低于 A_init，不意味着攻击者无法退回旧模型。一个可信的 OPD 防御主张应测量包括起点在内的最佳可选 checkpoint，而不只报告最差或最后一个 checkpoint。

如果论文主张防止整个“SFT+OPD”知识获取，必须同时定义 SFT 数据由谁提供、攻击者是否能保留原始文本以及 SFT checkpoint、哪些阶段实际受到防御。我们保留 teacher 自己正常生成的高质量 CoT 时，攻击者仍可能利用这些文本做普通 SFT。因此现阶段适合声称针对 **OPD 评分通道** 的抵抗能力，不适合声称阻断所有蒸馏方式。

## 实验指标

对同一组题、相同生成和评分配置，记录：

- A0：项目 CoT SFT 前的原始学生。当前是 Qwen2.5-0.5B-Instruct，已经经过厂商指令训练；“项目SFT前”不是完全未进行任何SFT的预训练基座。
- As：项目 SFT 后、OPD 前的学生。
- Ac：同起点、同预算、原 teacher 的 OPD 学生。
- Ad：同起点、同预算、防御 teacher 的 OPD 学生。

用户当前强目标：Ad 尽量不高于 A0。次一级目标：Ad ≤ A0 + 0.5(As−A0)，即至少抵消一半项目 SFT 带来的提升；不是简单地把 As 除以2。不同SFT训练量对应各自As，不能共用同一个阈值。

建议论文主指标另行记录：G_clean=max(0,max_t A_clean(t)−A_init)，G_def=max(0,max_t A_def(t)−A_init)。只有 G_clean 明确为正时，才解释 1−G_def/G_clean 这样的增益抑制比例。否则分母接近0会让指标失真。探索期间先报告原始准确率和配对增量，避免制造夸大的抑制百分比。

teacher 保持须分别记录 greedy、普通 sampling 和原始 sampling。答案正确率与推理过程可靠性也应区分。当前最终数字奖励不自动证明每个推理步骤正确。

## 一张 GPU 的 baseline 计划

专用预约9817267/gpu023，当前截止2026-09-11 08:09:27 ET，等待现有kl_only整条评估流水线完成后接续。其余卡继续防御研究；不额外申请资源。

1. 审核并复用项目SFT前、完整SFT后、当前clean OPD的同口径历史预测。重新计算A0及一半增益阈值。
2. 选 GSM8K train7000:7128 作为研究验证集，运行前检查与SFT/OPD训练题无交集；用 test1000:1200 作后续确认。历史test0:200继续作为已有探索记录，不用来选新baseline参数。
3. 从完整5epoch SFT起点，试240步、lr1e-6的原7B teacher OPD；其余关键协议延续现有修正实现，记录120/240两个checkpoint。这是同时扩大预算与适度提高学习率的探索配方，不是单变量因果实验。
4. 若第一轮验证提升不足3个百分点，用同一770条CoT重新做2epoch SFT，保留1/2epoch checkpoint；按验证集距45%的距离选择较弱起点，真实准确率如实报告。短训练的余弦学习率日程随总步数改变，因此不能说是原5epoch训练的完全相同前缀。
5. 在该起点运行同预算OPD并比较；若出现有希望的增益，固定配置在新测试片区检查，并在时间允许时加一个训练种子。3个百分点只是探索触发阈值，不是稳定有效的证据。
6. 如仍无增益，专卡继续研究，例如学生上下文上的 GKD forward-KL、更多/更合适的训练prompt、教师来源或采样/截断问题。不要把约50%直接称为模型容量上限。达到约45→50只能证明该初始化的增益，不能掩盖完整SFT起点结果。

所有新baseline输出独立放在 results/baseline_20260911，不覆盖现有120步防御对照。训练脚本会校验共享实现哈希。只有找到正向baseline后，才将选定配方用于匹配的防御评估；不能让防御组和干净组使用不同训练预算。
